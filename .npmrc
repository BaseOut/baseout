# When @fortawesome/pro-* packages are needed, uncomment the next two lines
# and export FONTAWESOME_TOKEN in your shell. Free FA packages resolve from
# public npm without this redirect.
# @fortawesome:registry=https://npm.fontawesome.com/
# //npm.fontawesome.com/:_authToken=${FONTAWESOME_TOKEN}

# @opensided packages are VENDORED (file:apps/web/vendor/@opensided/*), not
# fetched from GitHub Packages — so no registry redirect and no NPM_TOKEN are
# needed. Every consumer declares "file:" in package.json and the lockfile
# resolves them from disk. Restore these two lines only if an @opensided
# package is ever published and consumed from the registry:
#   @opensided:registry=https://npm.pkg.github.com
#   //npm.pkg.github.com/:_authToken=${NPM_TOKEN}
