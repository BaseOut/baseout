# wrangler.jsonc is generated from wrangler.jsonc.example by scripts/launch.mjs.
# It contains the local Postgres connection string (sourced from .dev.vars)
# which must never be committed. Per CLAUDE.md §3.3 + PRD §20.1.
wrangler.jsonc

# Local backup output (CSVs). Written by the Trigger.dev backup-base task —
# R2 was removed; see trigger/tasks/_lib/local-fs-write.ts.
.backups/
