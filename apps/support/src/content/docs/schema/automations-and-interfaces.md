---
title: Automations and Interfaces
description: The parts of Airtable the API will not give us.
---

Airtable's API does not export automations or interfaces, so Baseout cannot back them up. What it
offers instead is a place to **record them by hand**: a per-base list of your automations and your
interfaces (with their pages nested underneath), each with a description, tags and a soft-delete.

This is documentation you maintain, not a capture. It will not restore an automation for you.

## Questions this page will answer

- Why can Baseout not back up my automations?
- What is actually stored here — and what is not?
- If I restore a base, do the automations come back? (No.)
- What are entity tags for?
- Which plan includes these tabs?

## Not written yet

Only the summary above. The plan requirement is recorded in the specs as "Growth+", a tier name that
does not match the tier list used elsewhere — it needs resolving before it is documented.
