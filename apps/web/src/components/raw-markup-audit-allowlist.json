{
  "src/views/BackupsListView.astro": [
    "daisyUI `table` primitive on the backup-history table: no Storybook table component exists, and the `.table` class supplies the cell padding the dense run table needs. Per CLAUDE.md §4.2 this is sanctioned daisyUI-direct-in-view (the audit's escape hatch), not a custom component."
  ],
  "src/views/SchemaView.astro": [
    "daisyUI `tabs`+`tab-content` for the Browse/Docs tab panels: the `ui/Tabs.astro` primitive renders header inputs only (no content panels), so CSS-only radio tabs are the sanctioned daisyUI-direct second tier (CLAUDE.md §4.2). The Docs panel hosts the DocsTab React island (shared-schema-docs §5).",
    "`badge`/`loading` chips built as innerHTML in the Browse-tab client script for dynamically-rendered tagged-document chips (incl. the removed-entity warning) and the entity-detail load spinner."
  ]
}
