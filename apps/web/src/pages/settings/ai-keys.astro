---
/**
 * /settings/ai-keys — folded into the unified Settings hub (web-settings promotion). AI provider
 * keys (BYOK, shared-ai-byok) now render in the hub's Developer tab via AiKeysPanel, wired to the
 * same live `/api/ai-keys` routes. This route stays as a stable redirect so existing deep links
 * keep working. The pre-hub page body is preserved at views/AiKeysView.legacy.astro (rollback).
 */
return Astro.redirect('/settings?tab=developer');
---
