/**
 * Openside staff identity schema — package entry.
 *
 * Re-exports schema and types so consumers can write either:
 *   import { staffUsers } from '@opensided/openside-identity-schema'
 * or:
 *   import { staffUsers } from '@opensided/openside-identity-schema/schema'
 */

export * from './schema'
export * from './types'
