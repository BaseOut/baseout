## Why

Box is a user-owned BYOS destination and needs an OAuth flow plus a proxy stream writer to land backup output. Source-of-truth: PRD §10; Features §6.1.

## What Changes

Box OAuth + proxy stream.

## Depends on

- [baseout-server-engine-core](../baseout-server-engine-core/)
