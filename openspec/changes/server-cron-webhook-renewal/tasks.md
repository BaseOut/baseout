# Implementation tasks

## 1. Schema mirror

- [x] 1.1 Confirm `apps/server/src/db/schema/airtable-webhooks.ts` mirror carries `expires_at`, `status` (`active | notifications_disabled | pending_reauth | inactive`), `last_renewed_at` per `server-instant-webhook` Phase A (canonical migration in `apps/web`); header comment names the source. → Mirror created (server-instant-webhook A.4, same session); header names `apps/web/src/db/schema/core.ts:991` + canonical migration `apps/web/drizzle/0030_airtable_webhooks.sql`. Carries all three renewal columns plus id/org/connection/base/airtable_webhook_id/mac_secret/last_ping_at/modified_at.
- [x] 1.2 Ensure it's in the engine schema barrel `apps/server/src/db/schema/index.ts`. → Exported (alongside `airtable-webhook-subscriptions`).

## 2. Airtable RPC wrappers

- [x] 2.1 New `apps/server/src/lib/airtable-webhook-renewal.ts`:
  - `refreshAirtableWebhook(baseId, webhookId, accessToken, fetchImpl?)` → POST `/v0/bases/:baseId/webhooks/:webhookId/refresh`; returns `{ expires_at } | { error: 'not_found' | 'unauthorized' | 'rate_limited' | string }`.
  - `toggleAirtableWebhookNotifications(baseId, webhookId, enable, accessToken, fetchImpl?)` → the toggle-notifications endpoint; same error mapping.
  → Done, with one shape deviation: outcomes are kind-tagged unions mirroring the house `airtable-refresh.ts` style (`{ kind: 'success', expiresAt } | { kind: 'not_found' | 'unauthorized' | 'rate_limited' | 'transient', … }`) rather than `{ error: string }`. Mapping: 404→not_found, 401/403→unauthorized, 429→rate_limited (retry-after parsed), 5xx/network→transient. Toggle endpoint is `POST …/webhooks/:id/enableNotifications` with body `{ enable }`.
- [x] 2.2 Vitest with stubbed fetch per upstream status code; assert error mapping for both wrappers. → Covered in `apps/server/tests/integration/webhook-renewal-pass.test.ts` (wrapper describes: URL/method/bearer-header/body assertions + 200/404/401/403/429/5xx/network-throw mappings; 2xx-with-missing-expirationTime still succeeds with `expiresAt: null`).

## 3. Pure-orchestration module

- [x] 3.1 TDD red: `apps/server/tests/integration/webhook-renewal-pass.test.ts`. Cases: happy renewal (`expires_at` + `last_renewed_at` update, status stays `active`), notifications re-enable (`notifications_disabled → active`), 404 → `pending_reauth`, 401/403 → `pending_reauth`, 5xx → row unchanged + retried next pass, toggle 5xx → stays `notifications_disabled`, no eligible rows → no-op. → Written first and watched fail (module-not-found + missing cron const), then implemented to green. All listed cases covered, plus: transient failure logs `webhook_renewal_failed_transient`, empty pass logs `webhook_renewal_no_eligible_rows`, unavailable connection token counts transient (row unchanged — deliberately NOT pending_reauth, so a transient DO failure can't permanently kill a no-retry row), and a throwing dep never aborts the pass. Frozen `now`, all deps injected. 19 tests.
- [x] 3.2 Implement `apps/server/src/lib/webhook-renewal.ts` — `runWebhookRenewalPass(deps)`; deps: `db`, `fetchImpl`, `now`, `decryptToken`. Sequential await at MVP scale. → Done with two deviations: (a) module lives at `apps/server/src/lib/cron/webhook-renewal.ts` (beside the sibling cron modules, matching `oauth-refresh-sweep.ts`); (b) deps are fully-injected seams `{ listExpiring, listNotificationsDisabled, getConnectionToken, refresh, toggle, persistRenewal, persistStatus, log, now }` instead of `{ db, fetchImpl, now, decryptToken }` — matches the house sweep pattern and keeps the tests DB-free. Token access reuses the ConnectionDO `/token` gate (refresh-if-needed + legacy `encryptedToken` fallback, same as rediscovery run-deps) — no crypto reimplemented. Production wiring in `apps/server/src/lib/cron/webhook-renewal-deps.ts` (`runScheduledWebhookRenewal(env)`, per-firing DB client, 24h lookahead on `status='active'` rows; `expiresAt: null` from Airtable keeps the stored expiry rather than nulling the row out of the sweep). Returns counts `{ scanned, refreshed, reenabled, pendingReauth, transientFailures }`. Sequential awaits.

## 4. Cron activation

- [x] 4.1 Uncomment the webhook-renewal cron line in `apps/server/wrangler.jsonc.example` (`0 * * * *`). → There was no commented line inside `env.dev` to uncomment — added `"0 * * * *"` to `env.dev.triggers.crons` (dev is the only live env block). The top-level commented placeholder said `0 6 * * *` (daily); corrected to `0 * * * *` hourly per this proposal. Rendered `wrangler.jsonc` is gitignored and re-rendered by `scripts/launch.mjs` on next dev/deploy.
- [x] 4.2 Route the hourly cron to `runWebhookRenewalPass` in the `scheduled` dispatcher. → `src/lib/cron/dispatch.ts`: new `WEBHOOK_RENEWAL_CRON = "0 * * * *"` mapped to job `'webhook-renewal'` (existing `*/15` and `0 13` mappings untouched). `src/index.ts` `scheduled()`: `webhook-renewal` branch runs `withServiceRun(db, "webhook_renewal", …runScheduledWebhookRenewal(env))`. `webhook_renewal` promoted from `SERVICE_IDS.reserved` to `live` in `src/lib/service-runs.ts` with counts documented (`{ scanned, refreshed, reenabled, pendingReauth, transientFailures }`); the stale `reserved`-assertion in `tests/integration/service-runs.test.ts` updated to match.
- [x] 4.3 Miniflare scheduled-event test: simulate `cron: "0 * * * *"`, assert the pass runs. → Covered at the house-pattern seam instead: `tests/integration/cron-dispatch.test.ts` extended to assert `resolveCronJobs("0 * * * *") === ['webhook-renewal']` + cron-string distinctness (no Miniflare scheduled-event simulation exists for the other crons either; the dispatch map is the tested seam, matching server-oauth-refresh-cron-health).

## 5. Verification

- [x] 5.1 `pnpm --filter @baseout/server typecheck && test` — green. → `typecheck` green. Full `test` intentionally NOT run (known DO-teardown hang in local dev — see repo memory); targeted suites green instead: `vitest run tests/integration/webhook-renewal-pass.test.ts tests/integration/cron-dispatch.test.ts tests/integration/service-runs.test.ts` → 3 files, 38 tests passed.
- [x] 5.2 Document the cron in the apps/server cron-services docs section. → No standalone docs file exists; the living registry is the `dispatch.ts` header/constants (new hourly entry documented there) + the `SERVICE_IDS` counts comment block in `service-runs.ts` (webhook_renewal counts line added).
- [ ] 5.3 Smoke: seed a row with `expires_at = NOW() + INTERVAL '23 hours'` and another with `status='notifications_disabled'`; `wrangler dev --test-scheduled`; confirm the first's `expires_at` advances and the second returns to `active`. → Human smoke pending (needs a registered webhook so the Airtable refresh RPC has something real to hit; registration lands with server-instant-webhook Phase E).

## 6. Documentation

- [ ] 6.1 Update `specreview/04-recommendations.md` Round 4 — mark `server-cron-webhook-renewal` as now-active. → Deferred: `specreview/` is outside this session's allowed write scope (apps/server/** + the two change tasks.md only).
