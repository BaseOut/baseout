## Phase A — Schema

- [x] A.1 Generate the canonical migration in `apps/web/drizzle/` per design.md §Phase A: `airtable_webhooks` (org-level registry, UNIQUE (organization_id, base_id)), `airtable_webhook_subscriptions` (per-Space cursor + watermark), `backup_configurations.webhook_poll_interval_seconds`. → Canonical migration is `apps/web/drizzle/0030_airtable_webhooks.sql` (both tables + the poll-interval column, UNIQUEs + partial expiry index included).
- [x] A.2 Apply migration; verify tables landed. → 0030 applied to the dev DB.
- [x] A.3 Update `apps/web/src/db/schema/core.ts` with `airtableWebhooks` + `airtableWebhookSubscriptions`. → Landed at the bottom of `core.ts` (`airtableWebhooks` at :991, `airtableWebhookSubscriptions` at :1025) plus `backupConfigurations.webhookPollIntervalSeconds` (:530).
- [x] A.4 Engine mirrors `apps/server/src/db/schema/airtable-webhooks.ts` + `airtable-webhook-subscriptions.ts` (header comments name the canonical migration). → Done per the `backup-runs.ts` house convention (header names `core.ts:991` / `core.ts:1025` + migration `0030_airtable_webhooks.sql`; engine-needed columns only — `created_at` / `last_ping_source_ip` omitted; never migrated from this side). Also added `webhookPollIntervalSeconds` (integer, notNull, default 900) to the existing `backup-configurations.ts` mirror and exported both new mirrors from the barrel `apps/server/src/db/schema/index.ts`.
- [ ] A.5 Publish via `@baseout/db-schema` so `apps/hooks` can consume the registry table. → Superseded: following the `api_tokens` precedent instead — canonical stays in `apps/web/src/db/schema/core.ts`, each consumer mirrors locally. `apps/hooks` will carry its own mirror (read + `last_ping_at` stamp) when it lands; no `@baseout/db-schema` publish.

## Phase C — SpaceDO cadence polling

- [x] C.1 Update `apps/server/src/durable-objects/SpaceDO.ts`:
  - Add `next_webhook_poll_ms` alarm state; extend the single-alarm min-dispatch (cron fire vs webhook poll) per design.md §Phase C.
  - Poll interval from `backup_configurations.webhook_poll_interval_seconds` + 0–10% jitter.
  → Storage keys `webhook_poll_fire` (next fire, unix-ms) + `webhook_inflight` ({baseId: runId} for the in-flight guard). `alarm()` dispatches both lanes and re-arms at `minDefined(nextAlarm(fires), nextWebhookPoll)`; `/set-frequency` now also mins against a stored poll fire so a schedule change can't clobber it. New DO surface `POST /set-webhook-polling { enabled }` (called by the Phase E routes) arms/disarms; `alarm()` also self-arms whenever the config says `instant` (storage-drift self-heal) and drops the state when it doesn't. Jitter via injected `deps.random` (`computeNextWebhookPollMs`, `lib/webhooks/poll.ts`).
- [x] C.2 Implement the dirty-check query (dirty OR 24h safety sweep) against the master DB from the alarm handler. → `fetchDueWebhookSubscriptions` in `productionDeps` — the spec's exact WHERE (`last_ping_at > COALESCE(last_polled_at,'epoch') OR COALESCE(last_polled_at,'epoch') < now()-24h`, sweep cutoff passed as `.toISOString()` per the postgres-js raw-fragment trap). `decideWebhookPolls` re-applies the predicate defensively so the semantics live in one unit-tested place.
- [x] C.3 Per dirty subscription: in-flight guard → `backup_runs` INSERT (`triggered_by='webhook'`) → enqueue `incremental-backup` task → `last_polled_at = now()`. → In-flight guard = DO's `webhook_inflight` map reconciled against `backup_runs.status` (`queued`/`running` = in flight; `backup_run_bases` rows don't exist until completion so a run-row check via join was unusable). Run rows INSERT with **`kind='incremental'`** (a payload-apply is neither a `full` snapshot nor a `schema` capture; design.md updated). Enqueue-failure rolls back the row (`deleteRun`) and leaves the watermark unstamped so the next tick retries; skips (in-flight/paused `pending_reauth`/`inactive` webhooks) also don't stamp. `markWebhookRunStarted` flips the row to `running` + `trigger_run_ids=[handle.id]` so the standard `/complete` + reconciliation contracts apply.
- [x] C.4 Set `reconcile=true` in the task payload when `last_reconciled_at` older than 7 days. → `needsReconcile` (`RECONCILE_CADENCE_MS` = 7d; NULL ⇒ true).
- [x] C.5 Tests (TDD red first, `runInDurableObject`): dirty → enqueue; clean → no-op; safety-sweep fires with no ping; in-flight skip; ping-during-processing re-polls next tick; alarm coexistence with cron fire. → `tests/integration/webhooks/poll-decision.test.ts` (pure: dirty/clean/sweep/never-polled/ping-race/reconcile/jitter/paused/in-flight) + `tests/integration/space-do-webhook-poll.test.ts` (DO wiring: arm/disarm/min-arming, fire→enqueue payload shape, watermark, in-flight retained/pruned, rollback, cron-coexistence, self-arm). Existing `space-do.test.ts` "instant clears the alarm" expectation updated — instant now arms the poll lane instead of leaving no alarm.

## Phase D — Run plumbing

- [x] D.1 `POST /api/internal/webhook-subscriptions/:id/cursor` — monotonic cursor advance (reject decreases). → `pages/api/internal/webhook-subscriptions/cursor.ts`. Atomic CAS UPDATE (`WHERE payload_cursor <= :cursor`) so stale retries can't rewind: advance/equal-replay → 200 `{ok,cursor}`; decrease → 409 `{error:'cursor_regression', cursor:<stored>}`; unknown id → 404 `{error:'subscription_not_found'}`; bad body (cursor must be a positive integer) → 400.
- [x] D.2 `POST /api/internal/webhook-subscriptions/:id/fallback` — enqueue full `backup-base` for the base, stamp `last_reconciled_at`, reset cursor to Airtable's latest. → `pages/api/internal/webhook-subscriptions/fallback.ts`. Body `{reason}`. Inserts a `backup_runs` row (`triggered_by='webhook'`, `kind='full'`) and drives `processRunStart` with `fetchIncludedBases` narrowed to the affected base (single-base fan-out over the standard validated path; failure rolls the row back and mirrors runs/start's 404/409/422 mapping). Stamps `last_reconciled_at`, then best-effort cursor reset: token via the ConnectionDO `/token` gate (`lib/connections/token-via-do.ts`, extracted from webhook-renewal-deps) + `cursorForNextPayload` from Airtable's list-webhooks endpoint (`fetchAirtableWebhookCursor`) — "Airtable's latest". When the fetch can't run (dead token / webhook gone) the response carries `cursorReset:false` and the stale cursor stands (next poll re-signals). Success → 202 `{ok, runId, reconciledAt, cursorReset, cursor?}`.
- [x] D.3 Tests: cursor monotonic guard; fallback enqueues full run; both routes `INTERNAL_TOKEN`-gated. → Route shape per the house pattern: `webhook-subscriptions-cursor-route.test.ts` + `webhook-subscriptions-fallback-route.test.ts` (401 token gate / 405 / 400 UUID / 400 body variants). The monotonic CAS + fallback DB/Airtable happy paths need live PG → smoke checklist (H.3). ⚠️ Flagged gap (workflows side): `incremental-backup.task.ts` posts `/runs/:runId/complete` with `{created,updated,deleted,reconciledRecords,...}` but the complete route's parseBody requires `tablesProcessed/recordsProcessed/attachmentsProcessed` → its completion currently 400s. Widening the complete contract belongs to `workflows-instant-webhook` (or a shared follow-up) — not in this phase's task list.

## Phase E — Webhook lifecycle

- [x] E.1 Engine route `POST /api/internal/spaces/:id/register-webhooks` — find-or-create per included base: reuse active `(org, base)` row (subscription-only INSERT) or Airtable create with the full specification (dataTypes: tableData/tableFields/tableMetadata; includes: cell values "all", previous cell values, previous field definitions) and `notificationUrl` embedding the pre-generated row uuid. → Pure orchestration `lib/webhooks/lifecycle.ts` (`registerWebhooksForSpace`) + thin handler `pages/api/internal/spaces/register-webhooks.ts`; Airtable RPCs in `lib/webhooks/airtable-webhook-api.ts` (`WEBHOOK_SPECIFICATION` pinned by test). Branches: status≠'inactive' row → subscription-only (`reused`, works with a dead token); 'inactive' row → row DELETE (frees the UNIQUE (org,base) slot; its subscriptions are gone by definition) + fresh create (`recreated`); none → create (`created`). Subscription INSERT is `onConflictDoNothing` (idempotent re-enable). On success the route arms the DO poll lane via `/set-webhook-polling {enabled:true}` (best-effort; alarm self-arms too) → 200 `{ok, results:[{baseId,outcome}], pollingArmed}`.
- [x] E.2 Encrypt + persist `macSecretBase64` immediately (unrecoverable later); compensating Airtable DELETE if the row INSERT fails. → `encryptToken(secret, env.BASEOUT_ENCRYPTION_KEY)` (existing lib/crypto.ts; no new crypto) and the row INSERT happens before the subscription INSERT; insert failure → compensating `DELETE /v0/bases/:b/webhooks/:id` + 502 `{error:'registry_insert_failed', baseId}` (ordering + compensation pinned in lifecycle.test.ts).
- [x] E.3 Engine route `POST /api/internal/spaces/:id/unregister-webhooks` — delete subscriptions; Airtable DELETE + `status='inactive'` when the last subscription goes. → `unregisterWebhooksForSpace` + `pages/api/internal/spaces/unregister-webhooks.ts`. Deactivation proceeds even when the Airtable delete can't run (dead token / 404 — orphan pings 410 at hooks, logged). Disarms the DO poll lane (`{enabled:false}`, best-effort). → 200 `{ok, removedSubscriptions, deactivatedWebhooks}`.
- [x] E.4 Map Airtable's 2-per-base-per-integration cap error to `{ error: 'airtable_webhook_cap_reached' }`. → `createAirtableWebhook` matches cap language (`too many|limit|maximum|quota`) on 4xx error type/message → `cap_reached`; route surfaces 409 `{error:'airtable_webhook_cap_reached', baseId}` with no partial registry state for that base (earlier bases' registrations stand — valid + idempotent on retry).
- [ ] E.5 Wire apps/web's backup-config PATCH: transition to `instant` → register; away → unregister (route calls only; UI in `web-instant-webhook`). → SKIPPED this pass: requires `apps/web` edits, which are outside this change-pass's allowed paths (engine + change docs only). The engine routes + response shapes above are the ready contract; web's PATCH should also keep proxying `/set-frequency` (instant computes no cron fires) and rely on register/unregister for the poll lane.
- [x] E.6 Connection-disconnect path: subscriptions' webhooks with that `connection_id` → `pending_reauth`; reconnect path re-creates or re-points per design.md §Phase E. → Wired minimally at the engine's connection-invalidation point: `runScheduledConnectionInvalidation` (lib/cron/oauth-refresh-deps.ts) now flips `airtable_webhooks` rows of newly-invalidated connections to `pending_reauth` (status≠'inactive' guard). Earlier in the decay (`pending_reauth` connection), the hourly renewal cron already parks webhooks on 401/403. The poll lane skips `pending_reauth` webhooks (`skip_paused`). apps/web's user-initiated disconnect is a web-side touch → rides E.5's follow-up. Reconnect re-creation: an org reconnect yields an active Connection; re-running register-webhooks recreates 'inactive' rows, but `pending_reauth` rows currently need the renewal cron's refresh-or-404 path or a re-register after unregister — full reconnect re-point/re-create automation stays open under E.7's verification.
- [ ] E.7 Implementation-time verification: can a different same-org Connection's token refresh/poll a webhook created by another token? Record the answer in design.md and adjust E.6. → PARTIAL: docs-based expectation recorded in design.md §Phase E (webhook visibility is scoped to the OAuth integration, not the individual token — re-pointing `connection_id` is expected to work); needs a live two-Connection smoke to confirm before automating the reconnect re-point. No code adjustment made pending that smoke. → 2026-07-23 probe attempt: dev org `e9ae1e3f…` has only ONE active Airtable Connection — the live probe is blocked until a second dev Connection (different Airtable account, shared base) is connected. Nothing ticked.
- [x] E.8 Tests: find-or-create both branches; second Space reuses; last-unsubscribe deletes; cap error; compensating delete; reauth transitions. → `tests/integration/webhooks/lifecycle.test.ts` (13: create/reuse/recreate/cap/compensate/multi-base reuse/preconditions/unregister last-vs-not/404/no-token/no-op) + `tests/integration/webhooks/airtable-webhook-api.test.ts` (9: specification shape, cap mapping, error mapping, garbled-body guard, delete, cursor listing) + `tests/integration/spaces-register-webhooks-route.test.ts` (route shape 401/405/400 for both routes). Reauth transition covered by the E.6 hook + existing renewal-pass tests (`webhook-renewal-pass.test.ts` unauthorized→pending_reauth).

## Phase G — Doc sync

- [x] G.1 Confirm [openspec/changes/server/specs/airtable-webhook-polling/spec.md](../server/specs/airtable-webhook-polling/spec.md) matches the shipped behavior; link this change. → 2026-07-23: confirmed with one correction — the gap-detection requirement claimed reconciliation-found misses trigger a full re-read; as shipped they're corrected in-task by the reconciliation pass (full re-read = cursor expiry / INVALID_HOOK / INVALID_FILTERS only). Requirement text amended + linked to this change's Phase D route and `workflows-instant-webhook` task 1.8. Other four requirements match as written (this change was already linked from requirement 1).
- [x] G.2 Update [openspec/changes/server-schedule-and-cancel/proposal.md](../server-schedule-and-cancel/proposal.md) Out-of-Scope — link as resolved. → Out-of-Scope row marked RESOLVED 2026-07-23 with links to this change + `workflows-instant-webhook`, noting the Pro+ tier ruling it recorded is honored.
- [x] G.3 Update [shared/Baseout_Features.md §6.1](../../../shared/Baseout_Features.md): Instant = Pro+ per PRD; document per-tier `webhook_poll_interval_seconds` minimums. → §6.1 Instant row flipped Business+ → Pro+ with a PRD-§2.2/§9 ruling note, plus a "Webhook poll-interval minimums (provisional)" table recording the values shipped in web's `TIER_CAPABILITIES.webhookPollMinSeconds`: pro 900 s / business 300 s / enterprise 60 s (default 900; below-floor PATCH → `webhook_poll_interval_below_minimum`). Consistency cells updated in the same pass: naming dictionary "Instant Backup" note, §4.1 matrix Backup Frequency + Instant Backup rows (Pro column), §4.2 Instant (Webhook) row.
- [x] G.4 PRD change-log note: §2.5 DO-coalescing description superseded by dirty-flag registry + per-Space cadence polling. → Landed as an implementation note under **PRD §2.7** (Webhook-Based Incremental — the section that actually carries the superseded "per-client change log table + scheduled sync job" description; §2.5 is Failure Alerting and the "coalesce + debounce" wording doesn't appear in PRD v1.1): no stored events, no forward/debounce hop — dirty-flag registry + per-Space cadence polling + per-subscription cursor, pointing at this change's design.md.

## Phase H — Final verification

- [ ] H.1 `pnpm --filter @baseout/server typecheck && pnpm --filter @baseout/server test` — green.
- [ ] H.2 `pnpm --filter @baseout/web typecheck && pnpm --filter @baseout/web test:unit` — green (migration + schema only; UI is `web-instant-webhook`).
- [ ] H.3 Human checkpoint smoke:
  - Pro Space with dynamic DB ready. PATCH frequency=instant. Confirm `airtable_webhooks` + subscription rows and the webhook visible in Airtable's developer console.
  - Second Space (same org) enables the same base → no new Airtable webhook; new subscription row only.
  - Add a record in the source base. Within one poll interval, a `triggered_by='webhook'` run appears; per-space DB has the change.
  - PATCH both Spaces away from instant → Airtable webhook deleted; row `status='inactive'`.
- [ ] H.4 On approval: stage by name, commit locally.

## Out of this change (follow-ups, file separately)

- [ ] OUT-1 `server-instant-conflict-resolution` — payload-vs-dynamic-DB conflict handling (MVP: last-write-wins).
- [ ] OUT-2 `server-instant-snapshot-rollup` — periodic consolidation snapshots after N incrementals.
- [ ] OUT-3 `server-webhook-cursor-monitoring` — alert when a subscription's cursor nears the 7-day payload-retention edge.
- [ ] OUT-4 Central scanner cron (single dirty-scan ticking due SpaceDOs) — only if per-Space polling load demands.
