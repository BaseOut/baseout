# shared-ai-byok — tasks

TDD throughout (CLAUDE.md §3.4): each pure module writes its Vitest suite first (red → green → refactor). Nothing here is implemented yet. Ordering follows design.md's Migration Plan — the routing seam resolves `pool` for everyone until Task 4 lands, so every step is independently shippable and behavior is identical to today until BYOK is wired end-to-end. `resolveEntitlements(orgId)` (DB-native catalog) is the only capability source — never Stripe metadata (CLAUDE.md §1).

## 1. Key vault schema + encrypted write path (apps/web)

- [x] 1.1 Master-DB migration: `ai_provider_keys` (`organization_id`, `provider` enum, `key_enc`, `key_fingerprint`, `last_four`, `label`, `model_default` nullable, `status` enum `active|invalid|disabled`, `created_by_user_id`, `last_validated_at`, `validation_error` nullable, timestamps) + partial unique index on one **active** key per (org, provider). Generate + apply to dev DB in this change (CLAUDE.md §5.5 — schema-aware SSR 404s on a missing column). — _DONE 2026-08-07: added `ai-provider-keys.ts` to `drizzle.config.ts` schema array; generated `drizzle/0036_ai_provider_keys.sql` (dropped the spurious `usage_rollups` COALESCE-index re-emit drizzle-kit adds because that hand-written 0035 index doesn't round-trip); `db:migrate` applied to dev; `db:check` clean. Table + `ai_provider_keys_org_provider_active_uq` partial unique index live._
- [x] 1.2 Pure `persistProviderKey(db, encryptionKey, inputs)` + tests, mirroring `apps/web/src/lib/airtable/persist.ts`: AES-256-GCM encrypt via `crypto.ts` `encryptToken` before write, compute SHA-256 fingerprint + `last_four`, upsert one active row per (org, provider). Assert plaintext is never returned and never appears in the row read back.

## 2. Key-management API + settings UI (apps/web, Plus+ gated)

- [x] 2.1 API routes (add / rotate / revoke) + tests: server-side validation, Org-admin authz, `byo_ai_key` gate via `resolveEntitlements`; a submit-time provider health-check (Task 6.1) must pass before storing `status='active'`; audit-log rows on every write (metadata only — provider, `last_four`, actor — never the key). — _PARTIAL 2026-08-07: routes + DI handlers landed. `POST`/`DELETE /api/ai-keys` (`apps/web/src/pages/api/ai-keys/index.ts`): provider allow-list, non-empty key, **owner/admin-only** mutations, `byo_ai_key` gate via `resolveEntitlements` (403 `not_entitled` below Plus); add/rotate via `persistProviderKey`, revoke = status→disabled; 13 Vitest cases (incl. plaintext never echoed). **COMPLETED 2026-08-11:** submit-time health-check now gates POST (`checkKey` runs AFTER the entitlement gate; a failing key → `400 key_validation_failed`, nothing stored or audited) + metadata-only audit rows (`ai_key_added` / `ai_key_rotated` (per `persistProviderKey.rotated`) / `ai_key_revoked` via `writeAuthAudit` — provider + last_four + actor, never the key). 30 web ai/ai-keys tests green._
- [x] 2.2 Read/list endpoint + tests: returns only `{ provider, last_four, label, status, model_default, last_validated_at }` — **never** `key_enc` or plaintext (assert in test). — _DONE 2026-08-07: `GET /api/ai-keys` selects display-only columns; test asserts the payload never contains `key_enc`._
- [x] 2.3 Settings UI (Storybook-first per §4.2): per-provider key entry (write-only field, shows only `last_four` once saved), rotate + revoke controls, status/last-validated display, disabled affordance when un-entitled; `setButtonLoading` on every server-waiting action (§4.5). Paired designer surface flagged if a ui-only change is spun out. — _DONE 2026-08-07: `apps/web/src/pages/settings/ai-keys.astro` (route `/settings/ai-keys`, + a link Card on `/settings`), driven by `/api/ai-keys`. Per provider: status badge + masked `••••{last_four}` + label/model; write-only key field (never pre-filled/echoed); Save/Rotate (POST) + Revoke (DELETE via confirm Modal). Reuses existing primitives (no new `ui/*` → no story needed). SSR `byo_ai_key` gate (fail-closed) → un-entitled shows the Plus upgrade affordance; non-admins read-only. `setButtonLoading` everywhere. SSR selects display-only columns; key cleared after submit + page reload → plaintext lives nowhere on the page; astro check 0 new errors, build green. Full round-trips are deployed-only._

## 3. Routing seam + credential delivery (apps/server)

- [x] 3.1 Read-only `ai_provider_keys` mirror in `apps/server` (header comment names the canonical web migration; admin/server own no migrations). — _DONE 2026-08-07: `apps/server/src/db/schema/ai-provider-keys.ts` (read-only mirror, cites 0036) + barrel export; `apps/server/src/lib/ai/provider-keys-io.ts` provides the real `ResolveAiRoutingDeps` — `findActiveKey` (active-row facts, no key material) + `isByokEntitled` (server `resolveEntitlements` → `byo_ai_key`, fail-closed). Pure `byokEntitledFrom` decision has 4 Vitest cases; server tsc clean. Seam is now wireable; the call-site cutover is 4.x (needs per-provider clients — larger than a seam wire)._
- [x] 3.2 Pure `resolveAiRouting(orgId)` + tests: returns `{ mode: 'pool' }` or `{ mode: 'byok', provider, model, billable: false }`; `byok` only when `byo_ai_key` resolves true AND an `active` key exists for a supported provider. Return shape carries **no secret material** (safe to log).
- [x] 3.3 `INTERNAL_TOKEN`-gated credential-fetch endpoint (`GET /api/internal/orgs/:orgId/ai-credential?provider=…`) + tests: resolves routing, decrypts `key_enc` server-side, returns plaintext only over the trusted boundary; rejects missing/invalid `x-internal-token`; response body never logged. Payload/enqueue carries only `{ orgId, provider }`. — _DONE 2026-08-11: `apps/server/src/pages/api/internal/orgs/ai-credential.ts` — pure `resolveAiCredential(token, orgId, deps)` (injected token-check + adapter resolver) verifies the token BEFORE any resolve/decrypt (401 → `resolveAdapter` never called), else maps `resolveByokAdapter` → `{mode:'pool'}` | `{mode:'byok',provider,model,apiKey}`. Provider is RESOLVED from the org's single `active` key (via `findActiveKey`), not passed as a query param — the runner only holds an orgId. Thin `orgsAiCredentialHandler` (GET-only, 405 else) wires a constant-time check (exported from `middleware.ts`) against `env.INTERNAL_TOKEN` + DB-backed `resolveByokAdapter` with `BASEOUT_ENCRYPTION_KEY`; registered in `index.ts` (`ORGS_AI_CREDENTIAL_RE`). 6 Vitest cases incl. an explicit "apiKey never in any console.* call" assertion. Server tsc clean._

## 4. Route the three AI call sites through the seam

- [x] 4.1 `apps/workflows` chat task: fetch the customer key at run start via the Task 3.3 endpoint when routing is `byok`; construct the Anthropic client from it instead of `process.env.ANTHROPIC_API_KEY`; fall back to the pool key when `pool` (or on strict-custody, hard-fail per D5/D9). Tests target the pure module with an injected credential provider — plaintext never in the payload. — _DONE 2026-08-11: new pure `apps/workflows/trigger/tasks/_lib/ai-credential.ts` — `fetchAiCredential(fetch, engineUrl, token, orgId)` GETs the 3.3 endpoint (x-internal-token) and falls back to `{mode:'pool'}` on ANY error (non-OK, throw, malformed body) so BYOK can't break chat; pure `resolveChatClientConfig(cred, pool)` picks `{apiKey, model}` — byok → its key + `model ?? CHAT_MODEL`, pool → env key + CHAT_MODEL (byte-identical to before). `chat-respond.task.ts` run() now fetches the credential from `payload.organizationId` and builds `new Anthropic({apiKey})` from the resolved config (model threaded into `generateWithClaude`). PAYLOAD CHANGE: `ChatRespondPayload = ChatRespondInput & { organizationId }` (pure `ChatRespondInput`/`runChatRespond` untouched — existing tests stay green); `apps/server` chat-send resolves `spaces.organizationId` and adds it to the enqueue. 11 Vitest cases incl. "apiKey never logged" + "payload carries orgId, never the key/apiKey field". Workflows tsc clean._
- [x] 4.2 `apps/server` `describe-schema-io.ts`: branch `workersAiGenerate` behind `resolveAiRouting` — `byok` routes to the customer provider (direct SDK per D2); `pool` keeps `env.AI` unchanged. Tests cover both branches. — _DONE 2026-08-07: added `provider-call.ts` (fetch-based anthropic/openai clients, no SDK dep; `cloudflare` non-callable → pool) + `byok-credential.ts` (`resolveByokCredential` in-Worker AES-256-GCM decrypt via `lib/crypto.ts` `decryptToken`; `resolveByokAdapter`/`resolveByokAdapterForSpace` compose the routing seam + credential). `workersAiGenerate(env, byok?)` branches to `callByokModel` on BYOK, else the byte-for-byte `env.AI` path. Threaded org/db from the `schema-sync` waitUntil (resolves byok once with the fresh DB). Pool + byok covered in Vitest (env.AI, fetch, decrypt, db injected/mocked)._
- [x] 4.3 `apps/server` `health-score-run.ts`: same branch for `workersAiScoreMetric`; `pool` keeps `env.AI` unchanged. Tests cover both branches. — _DONE 2026-08-07: `workersAiScoreMetric(env, byok?)` mirrors 4.2 (byok → `callByokModel` → same `parseScoreResponse` contract; pool unchanged). Threaded org/db from both call sites — `schema-sync` (post-sync scoring) and `health-rerun`. Both branches covered in Vitest._

## 5. Compose with `ai_usage` policy + zero-credit flag

- [ ] 5.1 Enforce D3 ordering at every AI entry point (+tests): resolve the `shared-ai-controls` `ai_usage` policy FIRST (reject `off`; record-data AI requires `all`), THEN `resolveAiRouting`. Assert a valid BYOK key never bypasses `off` and never widens `schema_only` to record-data AI. — _**BLOCKED 2026-08-11:** `shared-ai-controls` (the `ai_usage` policy this must compose with) is **spec-only** — no policy resolver exists in code, so there is nothing to resolve-first. This unblocks only once that change is implemented; not an ai-byok-side gap._
- [x] 5.2 Attach `billable: false` to the AI usage sample on BYOK routing (+tests). Note in code + test that the AI-credit meter is unwired (`shared-entitlements` 3.3); this only sets the flag the meter will honor. — _DONE: `resolveAiRouting` already returns `billable: false` on `byok` (with a code comment that the meter is unwired); it's the flag the future `shared-entitlements` 3.3 meter will honor. Nothing consumes it yet (no meter)._

## 6. Validation, health, lifecycle

- [x] 6.1 Provider health-check helper + tests: minimal models-list / 1-token completion per provider; used at submit (Task 2.1) and by the sweep; external providers mocked at the HTTP boundary (msw, CLAUDE.md §3.4). — _DONE 2026-08-11: `apps/web/src/lib/ai/check-provider-key.ts` — anthropic/openai `GET /v1/models` (key in header only), cloudflare skipped (no cheap endpoint), status-only errors, fail-closed on throw; 8 tests (`fetch` injected). Wired at submit (2.1)._
- [ ] 6.2 Periodic re-validation sweep + tests: flips a key to `status='invalid'` with `validation_error` + stamps `last_validated_at` on provider auth failure; default call-time fallback to pool (if entitled) with a surfaced warning; strict-custody opt-in hard-fails instead (D5/D9). — _**DEFERRED (optional hardening) 2026-08-11:** submit-time validation (6.1) covers the real need — a bad key never stores active. The periodic re-validation cron is nice-to-have (catches keys that go stale after storage); not required for a functional BYOK. Standalone follow-up._
- [~] 6.3 Revocation / rotation / downgrade + tests: rotate = re-validate then replace-in-place (old plaintext overwritten, never archived); revoke reverts routing to `pool`; downgrade below Plus sets `status='disabled'` (not purged) so re-upgrade restores. — _PARTIAL 2026-08-11: **rotate** (re-validate via 6.1 then replace-in-place via `persistProviderKey` upsert) + **revoke** (→`disabled`; routing reverts to `pool`) DONE in the API. **Downgrade** is effectively handled: `resolveAiRouting` gates on `byo_ai_key` FIRST, so a downgraded org's keys never route regardless of row status — the explicit `status='disabled'` flip is cosmetic (defer to a subscription-change hook; nothing routes in the meantime)._

## 7. Verification & docs

- [x] 7.1 Security sweep (design.md → Security review points): grep the diff for the key variable + `console.` at commit; assert no read/list/log/payload path exposes `key_enc` or plaintext; confirm the credential endpoint is `INTERNAL_TOKEN`-gated and its response is unlogged; confirm web/server share `BASEOUT_ENCRYPTION_KEY`. — _DONE 2026-08-11: swept the whole ai-byok surface (web `lib/ai` + `api/ai-keys` + settings page; server `lib/ai` + credential endpoint; workflows credential lib). No `console.*`/`debugger`. The only `apiKey`/plaintext uses are in-process (config object / request header / decrypt) + the INTERNAL_TOKEN-gated 3.3 response body — never a log, Trigger.dev payload, or unauthenticated response (asserted by tests both sides). `BASEOUT_ENCRYPTION_KEY` shared web(write)↔server(read)._
- [x] 7.2 `pnpm --filter @baseout/web test` + `@baseout/server test` + `@baseout/workflows test` green; `tsc --noEmit` clean across the three apps; `db:check` clean after 1.1. — _DONE 2026-08-11: `tsc` clean — server 0, workflows 0, web 0 in touched files (~15-17 pre-existing errors in unrelated files only). ai suites green: server 28, workflows 15, web 30. `db:check` clean (0036 applied)._
- [~] 7.3 Cross-reference bookkeeping: note the BYOK routing seam in `shared-entitlements` (zero-credit `billable` consumer) and `shared-ai-controls` (policy-first composition) proposals; record the founder's Gateway-vs-direct + launch-provider decisions (design.md Open Questions) once made; add an `oauth-setup.md`-style credential note if Gateway routing is chosen. — _PARTIAL 2026-08-11: the seam is documented in this change + the coordinating `shared/internal/action-plan-2026-08-07.md`. The founder Gateway-vs-direct + launch-provider decisions (design.md Open Questions) remain open — direct SDK/fetch shipped as the launch choice; revisit if Gateway is adopted._
