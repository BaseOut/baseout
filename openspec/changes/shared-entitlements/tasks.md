# shared-entitlements — tasks

TDD throughout (CLAUDE.md §3.4): every pure module gets its Vitest suite first. Enforcement ships behind `ENTITLEMENT_ENFORCEMENT` (default off) so schema/metering can land safely ahead of cutover. Cross-app payload changes (workflows callbacks ↔ server ingestion) update both sides in the same task.

## 1. Catalog schema + seed

> **Phase 1A built + applied 2026-08-04 (50 unit tests green; `astro check` clean for all 1A files).** Pure catalog tables live canonically in `@baseout/db-schema` (`src/schema/entitlements.ts` + `src/entitlements/values.ts`); account-scoped tables (which FK to organizations/spaces/users) in `apps/web/src/db/schema/entitlements.ts`; `subscription_items.plan_id` added in `core.ts`. Migration `apps/web/drizzle/0034_entitlements_catalog.sql`. Seed data + idempotent runner in `apps/web/src/db/seed/`; Stripe sync in `apps/web/src/lib/stripe/entitlement-catalog-sync.ts` + `scripts/setup-stripe-entitlements.ts`. **Applied to the shared dev DB:** `db:migrate` ✓, `db:seed:entitlements` ✓ (verified: 7 groups, 29 features, 6 plans, 9 prices, 174 plan_features, 15 add-ons, 2 fair-use nulls). **Still deferred:** `pnpm setup:stripe:entitlements` (blocked — no `STRIPE_SECRET_KEY` in `apps/web/.env`); server/admin mirror header-comments (added when 2.x/3.x consume the tables).

- [x] 1.1 `packages/db-schema`: table definitions — `plans`, `plan_prices`, `feature_groups`, `features`, `plan_features`, `addon_catalog` (feature slug, unit quantity, kind, price, Stripe price IDs — the locked add-on library as data), `account_feature_overrides`, `addon_purchases` (references `addon_catalog`), `usage_rollups`, `usage_notification_state`; `subscription_items.plan_id` reference; typed-value columns + enum-rank storage; tests for type/rank helpers
- [x] 1.2 Canonical migrations in `apps/web` (+ header-comment mirror updates queued for server/admin); `db:check` clean — _migration 0034 generated (purely additive) + applied to dev DB; server/admin mirrors deferred to 2.x/3.x consumers_
- [x] 1.3 Seed migration: 7 groups, all features (slug, type, unit, enum ranks, meter_kind), 4 public plans + trial + enterprise baseline, the `addon_catalog` library (guide §6), full `plan_features` matrix from `pricing-guide.md` §3 (cite guide version in the seed header); test asserting seeded matrix == a fixture derived from the guide — _seeded + verified in dev DB_
- [~] 1.4 Stripe setup script (idempotent, env-scoped): 4 products (+`plan_slug` metadata) × monthly/annual prices + add-on SKUs from guide §6; writes `plan_prices` / add-on price mapping — _pure spec builder + idempotent reconciler + CLI + tests done; running vs live Stripe blocked on `STRIPE_SECRET_KEY`_

## 2. Resolution + account entitlements

> **2.1 + the 2.3 backfill built + verified 2026-08-04 (25 tests green).** Pure composition `packages/db-schema/src/entitlements/resolve.ts` (`composeEntitlements` + typed accessors `getLimit`/`getBool`/`getEnum`/`isWithinLimit`; 17 tests). apps/web query wrapper `src/lib/entitlements/resolve.ts` (`resolveEntitlements(db, orgId)`; 2 tests). Legacy tier→plan-slug map `src/lib/entitlements/legacy-tier-map.ts` (price-aligned; 4 tests) + backfill script `scripts/backfill-plan-id.ts` (`db:backfill:plan-id`; 2 wrapper tests). **Verified end-to-end on the dev DB:** backfill mapped the one org's `tier=growth → plan_id(core)`, and `resolveEntitlements` returned planSlug=core with all 29 features at correct Core values.

- [x] 2.1 Pure resolution lib (shared: web + server + admin consume): `resolveEntitlements(orgId)` join + `effective()` rule (override ?? plan, add-on stacking on limits, enum rank comparisons, override expiry, fair-use sentinel); exhaustive Vitest on composition cases — _composition in @baseout/db-schema (all 3 apps consume); apps/web query wrapper done. server/admin thin wrappers land with their consumers._
- [~] 2.2 Webhook extensions in `apps/web`: subscription item ↔ `plan_id` maintenance, add-on purchase lifecycle (recurring items; one-time with `expires_at` = period end), idempotent by Stripe event ID; integration tests vs msw Stripe — _NOTE: no Stripe webhook receiver exists yet (only trial-sub creation in onboarding) — the receiver is from-scratch. Pure DI sync handlers built + tested: `src/lib/entitlements/webhook-sync.ts` — `syncSubscriptionEntitlements` (plan_id maintenance + recurring add-on upsert + deactivate-not-in) and `syncOneTimeAddons` (invoice packs, expiry injected per D6); 6 tests. **Remaining infra:** the receiver route + signature verify + idempotency events table (new migration) + msw integration + Stripe dashboard config (deployed-only for real signatures) — pairs with Phase 6 checkout, which also needs webhook confirmation._
- [~] 2.3 Backfill script: existing `subscription_items.tier` → `plan_id`; grep-audit and migrate all `tier`-based gating call sites to the resolution lib — _backfill mapping + script done + run + verified. **Call-site migration BUILT (2026-08-04), cutover pending a deliberate flip.** Product decision landed: bases cap is **org-wide** (`bases_under_management`); the per-Space gate is retired. Spec re-read resolved the earlier "catalog gap": the locked model has NO `schema_docs`/`webhook_poll_min` levers (docs gated by `documents` count; poll floor is operational), so those are **derived**, not seeded — no catalog change/re-seed. Shipped: pure `entitlementsToCapabilities` bridge (`apps/web/src/lib/capabilities/entitlement-capabilities.ts`) — basesPerSpace←`bases_under_management` (effective, add-ons stack), frequencies←`backup_frequency_max` down FREQUENCY_LADDER (trial one_time→no scheduled cadence), schemaDocs←`byo_ai_key`?manual_ai:manual, webhookPollMin←instant?300:900 — with 8 exhaustive per-plan tests; `resolveCapabilities` now takes `{preferEntitlements}` (default OFF, fail-safe fallback to the tier table on null/error) so production is byte-identical until the cutover flips the default. Typecheck clean on the touched files. **apps/web CUTOVER DONE 2026-08-04:** `preferEntitlements` now defaults **true** (`resolveCapabilities` header/option docs updated); smoke-verified against the backfilled Core org — `basesPerSpace 15→50` (the intended per-Space→org-wide change; more permissive, so nothing is wrongly blocked), `frequencies`/`schemaDocs`/`webhookPollMinSeconds` identical. **1006 apps/web unit tests pass, 0 typecheck errors in touched files** (test orgs aren't backfilled → hit the fail-safe legacy fallback → no regression). **REMAINING for full 2.3:** the **apps/server** cutover — server has its OWN `resolveCapabilities` mirror + engine consumers (`runs/start`, `retention/cleanup-deps`, `rediscovery`, `workspaces/auto-enroll`, `webhook-subscriptions/context`); needs a server-side `resolveEntitlements` wrapper + the same bridge + entitlement-table mirrors, then flip. Behavior-changing on the engine — a focused server pass._
- [~] 2.4 Override write path (staff-only route + lib): typed validation against the feature, required reason, audit row on every write; tests incl. audit assertions — _pure `validateOverrideValue` + `applyOverrideWrite` (DI domain write) in `@baseout/db-schema` (11 tests); composes with the existing `runAudited` door — apps/admin composition test proves intent+result audit rows precede the upsert (`AdminAction` gained `set_entitlement_override`); 2 tests. The staff-gated HTTP route + Drizzle deps (mirror-backed) land with admin-entitlements 3.2._

## 3. Usage metering

> **3.1 built + verified 2026-08-04.** Shared pure `currentMonthlyPeriod(anchor, now)` in `@baseout/db-schema` (`src/entitlements/period.ts`, 9 tests) — the monthly-anniversary cycle (D6) both stock buckets and flow resets (3.3/3.5) will reuse. Workflows: `runBackupBase` now accounts file bytes (CSV UTF-8 + newly-written attachment bytes; dedup skips excluded) → `BackupBaseResult.fileBytes`, threaded into the `/complete` POST as `fileBytesProcessed` (backup-base-file-bytes.test.ts, 4 tests; full workflows suite 399 green). Server: `usage_rollups` + `subscriptions.createdAt` mirrors; migration `0035_usage_rollups_unique_idx.sql` (COALESCE(space_id,'') unique index for atomic ON CONFLICT) generated + **applied to dev DB** (`db:migrate` ✓, `db:check` clean, index verified). Pure `src/lib/runs/usage-ingest.ts` (`buildUsageSamples` + DI'd `ingestRunUsage`, 6 tests) + Drizzle deps in the complete route (org+anchor resolve, ISO-cast raw upsert), gated to snapshot completions and skipped on noop replays. Route parse + 38 run-complete server tests green; all four typechecks clean.
>
> **Scope decisions (flagged per §3.1):** the authoritative usage-metering spec scenario carries **record count + file bytes** only — `bases_under_management` is seeded `meter_kind='creation'` (D6: creation caps `COUNT(*)` live, never roll up), so no base rollup is written here. Ingestion is a best-effort per-base **increment**; the reconciliation sweep (3.5) is the authority that re-derives exact stock levels, so lost callbacks (under-count) and re-backups (over-count) are bounded drift it heals. **The real-PG upsert + org-attribution SQL is unit-tested via DI but not integration-tested** (no local Docker PG; same deferral as the rest of the epic) — validated end-to-end in the 9.4 smoke.

- [x] 3.1 `apps/workflows` + `apps/server` (same task — shared contract): extend progress/complete callback payloads with records, file bytes, and base counts; server ingestion writes Space-attributed `usage_rollups`; tests both sides — _done 2026-08-04; see the note above_
- [x] 3.2 `apps/server`: per-Space database-size measurement at run finalization (PG `pg_database_size()`; D1 via Cloudflare REST API `file_size` — PRAGMAs unsupported) → stock rollup write — _done 2026-08-04. Pure `src/lib/runs/db-size.ts` (`measureSpaceDbSizeBytes` backend dispatch + `ingestSpaceDbSize`, 10 tests). Meter is `database_size_gb` (stock, org-wide; rows written per-Space so the org total is their SUM). Wired into the complete route on `result.kind === 'finalized'` only (once per run), best-effort. Shared `resolveOrgAnchor` + a mode-parameterized `upsertRollup(add|set)` extracted (2nd call site) — DB size is an absolute measurement so it uses **`set`** (a level), vs 3.1's per-base **`add`**. Real measurers: managed_pg = **schema-relation-size SUM** (`pg_total_relation_size` over `bo_space_<id>`), NOT `pg_database_size()` — the schema-per-Space topology (`space-db-pg.ts withSpaceSchema`; `pgLocator` IS the schema name) makes schema-size the correct measure; **spec-vs-impl reconciliation flagged**. D1 = CF REST `file_size`, gated on new optional `CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_D1_API_TOKEN` env (returns null until provisioned — the deferred d1-token item). byodb deferred with that backend. Server typecheck clean; 48 run-complete/usage/db-size tests green. **Real schema-size query + CF call unit-tested via DI, not integration-tested** (no local Docker PG / D1 token) — validated in the 9.4 smoke._
- [ ] 3.2a Documents count rollup maintained by the web document CRUD routes (+ sweep correction); enforcement check on document create against the effective cap
- [ ] 3.3 Point-of-use metering: AI-credit recording (cost × 125 per `ai-credit-model.md`, per-model rates table/config) and API/MCP/SQL call counting in the serving middleware; period-anchored flow rollups; tests
- [ ] 3.3a Burst rate limit via the Workers Rate Limiting binding keyed per org/token (D11a) on API/MCP/SQL routes; AI Gateway in front of Workers AI for observability/verification (logs, token analytics) — not accounting
- [ ] 3.4 Live-count resolution for creation caps (Spaces, bases, seats accepted-members, destinations, reports) inside the resolution/usage read path — no stored rollups; tests — _PARTIAL 2026-08-06: pure kernel landed. `summarizeCreationUsage` (creation-class limit features → used/limit/remaining/withinLimit/pct against injected live counts, sorted, missing count → 0) + `canCreate` (one-more-slot gate, fair-use always yes, fails open on absent/non-limit) in `packages/db-schema/src/entitlements/creation-usage.ts`, barrel-exported; 13 Vitest cases (`tests/entitlement-creation-usage.test.ts`). Consumed today by the create-time gate (4.3), and by the usage endpoint's live counts (9.1: Spaces + Seats). **REMAINING:** the remaining live-count I/O (destinations/bases/documents/reports — the latter three need an engine round-trip) + the Space-scope read path._
- [ ] 3.5 Reconciliation sweep (server cron): re-derive stock meters from durable rows, correct `usage_rollups`, handle period rollover closing/opening of flow rows; drift-healing test

## 4. Enforcement + notification skeletons

- [x] 4.1 Pure evaluator: `evaluate({used, limit, current, enforcementEnabled})` → transitions of `ok → warned_90 → warned_100 → enforced` (warn 90% / enforce 100%; `enforced` vs `warned_100` selected by the ENTITLEMENT_ENFORCEMENT flag per 4.3; fair-use/null limit never leaves ok; 0.05 de-escalation hysteresis band; returns the `NotificationKind` fired only on escalation for dedupe) + `resetForNewPeriod()`. Lives in `packages/db-schema/src/entitlements/evaluate.ts` (shared by server rollup ingestion + web), barrel-exported. 18 exhaustive Vitest cases (`tests/entitlement-evaluate.test.ts`), typecheck clean.
- [x] 4.2 Wire evaluation into every usage-write path (ingestion, point-of-use, sweep); persist `usage_notification_state`; transitions-only invoke `notifyLimitWarning` / `notifyLimitEnforced` skeletons (structured-logged no-ops with final signatures) — _core + engine wiring built + tested 2026-08-04; **run-complete call-site landed 2026-08-05** (see below)._ **Pure batch orchestration** `evaluateUsage` in `@baseout/db-schema` (`src/entitlements/usage-eval.ts`): drives `evaluate()` over a batch of (feature, used) observations against the resolved `EntitlementMap`, partitioning into `changed` (states to upsert) + `notifications` (transitions to dispatch); skips non-limit/unknown features; fair-use resets without alerting; 10 Vitest cases. **Engine half:** server `resolveEntitlements` (`src/lib/entitlements/resolve.ts` — the deferred 2.1 server wrapper; pure catalog tables imported from `@baseout/db-schema`, account tables via new read-only mirrors `account_feature_overrides` / `addon_purchases` / `usage_notification_state` + `subscription_items.plan_id` added to the existing mirror); skeleton notifiers `createSkeletonNotifier` (`notify.ts`, injected structured-log sink → no `console.*`, real email is 5.1); DI orchestration `evaluateUsageForOrg` (`usage-enforcement.ts`) = resolve → read usage+state → `evaluateUsage` → write only-moved states + dispatch only-transitions, behind `ENTITLEMENT_ENFORCEMENT` (default off → warn-only). 8 server tests; server typecheck clean; db-schema 91 / server 46 green. **DONE 2026-08-05:** (a) the run-complete call-site landed — after `ingestRunUsage`/`ingestSpaceDbSize`, on every non-noop snapshot completion it resolves the org anchor, computes the period via `currentMonthlyPeriod`, and calls `evaluateUsageForOrg` with Drizzle deps built on the now-landed 3.2 `resolveOrgAnchor` helper: `readUsage` = org-summed `usage_rollups` per period (query builder + `sum()`), `readStates` = `usage_notification_state` read, `writeStates` = ON-CONFLICT upsert on `usage_notification_state_org_feature_period_unique` (ISO-cast dates per the postgres-js raw-fragment trap); notifier = `createSkeletonNotifier()`; `enforcementEnabled = env.ENTITLEMENT_ENFORCEMENT === "1"`. Best-effort (try/catch — a metering failure never fails the wire completion); the sweep (3.5) is the authority. Meter superset `records_under_management`/`file_storage_gb`/`database_size_gb`; absent-rollup meters are silent no-ops (not returned by `readUsage`). (c) `ENTITLEMENT_ENFORCEMENT` declared in `apps/server/src/env.d.ts` (optional; unset = default-off = warn-only, same `.dev.vars`-only convention as `VIEW_CAPTURE_OVERRIDE`); actually flipping it on + the enforcement actions are task 4.3. Server typecheck + `tsup` build clean; run-complete/usage/db-size/enforcement suites 56 green, per-space 427 green. **REMAINING:** (b) the point-of-use (3.3) and sweep (3.5) call sites reuse the same `evaluateUsageForOrg`. Real-PG deps unit-tested via DI, not integration-tested (no local Docker PG) — the ON-CONFLICT upsert + org-summed read validated in the 9.4 smoke._
- [ ] 4.3 Enforcement actions behind `ENTITLEMENT_ENFORCEMENT` flag: scheduler DO pre-enqueue check (background pause at job boundary), interactive refusal payloads (AI + API middleware, 429 + add-on hint), creation-cap checks in mutating routes; restore-immunity test; auto-resume on add-on purchase/plan upgrade/cycle rollover — _PARTIAL 2026-08-06: creation-cap subset landed for the Space create action. `checkCreationCap` DI helper (`apps/web/src/lib/entitlements/enforce-create.ts`) resolves entitlements + a live count + `canCreate`, returns `{allowed, used, limit, addonSlug}`; behind `ENTITLEMENT_ENFORCEMENT` (default off → dark, no resolve/count work); fails open when there's no resolvable plan; 6 Vitest cases. Wired into `POST /api/spaces` (Spaces) and the join-request **approve** path `POST /api/organizations/join-requests/[requestId]` (Seats — a new accepted member; optional `checkSeatCap` dep at the route, `decideJoinRequest` untouched) — at cap each returns 403 `{code:'limit_reached', feature, used, limit, addon}`; existing rows untouched (guards the create only). `ENTITLEMENT_ENFORCEMENT?: string` declared on `Cloudflare.Env` in `apps/web/src/env.d.ts` (reads `=== "1"`, matching the engine). Web typecheck adds 0 new errors; no schema migration (pure logic). **REMAINING:** the other creation-cap sites (bases/destinations/reports/documents mutating routes), scheduler-DO background pause, interactive 429 refusals, an explicit restore-immunity test, and auto-resume._
- [ ] 4.4 Retention-window wiring: the cleanup/retention engine reads schema-history, record-history, and audit-log retention from `resolveEntitlements` (per-org effective values) instead of any hardcoded ladder; tests
- [ ] 4.5 Churn lifecycle: `past_due` = unchanged entitlements; cancellation = churn grace state (backups stop, data readable/restorable, deletion warnings, cleanup after the grace window, re-subscribe restores); grace window 30 days (locked, held as config); tests with fake clock

## 5. Notifications (email) + seat rule

- [ ] 5.1 Limit emails: React Email template (feature, used, limit, pct/overage, add-on/upgrade link) sent via Mailgun from the notifier call sites; structured-logged; tests with mocked transport
- [ ] 5.2 Seat/invite rule: limit state counts accepted members only; invite route blocks when accepted + outstanding ≥ effective seat limit with cancel/add-on/upgrade guidance; cancel-invite frees a slot; tests

## 6. Billing checkout (web)

- [ ] 6.1 Plan purchase flow: Stripe Elements payment capture, subscription create/update at the selected plan price, webhook-confirmed entitlement switch; msw-Stripe integration tests
- [ ] 6.2 Plan change with proration: upcoming-invoice preview before confirm; downgrade over-limit warning listing the specific meters; tests
- [ ] 6.3 Add-on purchase/cancel: recurring quantities + one-time packs (`expires_at` = period end); enforcement re-evaluation on confirmation (auto-resume); tests

## 7. Trial lifecycle

- [ ] 7.1 `trial_expires_at` set on first successful trial run completion (+14 days); upgrade cancels the clock; tests
- [ ] 7.2 Daily trial evaluation cron (server): staged T-7/T-3/T-1/day-of emails, deduplicated per stage; tests on the pure stage logic
- [ ] 7.3 Deletion job at expiry via existing cleanup machinery; deletion recorded/auditable; account remains usable; end-to-end test with fake clock

## 8. Legacy migration registry

- [ ] 8.1 `legacy_customers` table (schema + migration, per D13 — no import tooling): unique ci-email, legacy plan, mapped plan, export metadata, redemption state
- [ ] 8.2 Signup/checkout matching: verified-email lookup, UI surfacing payload (mapped tier + offer), at-or-above tier rule; tests
- [ ] 8.3 Redemption: apply the canonical 20%-forever Stripe coupon on checkout, mark row redeemed (single redemption), audit; msw-Stripe tests

## 9. Visibility + verification

- [ ] 9.1 `apps/web` endpoints: org-scope and Space-scope usage/limits payloads (effective limit, used, pct, state, period) from resolution lib + rollups; auth via middleware; tests — _PARTIAL 2026-08-06: org-scope endpoint landed. `GET /api/usage` (`apps/web/src/pages/api/usage/index.ts`, auth via middleware) returns `{planSlug, meters[]}` where each metered limit feature carries `{used, limit, remaining, withinLimit, pct, meterKind}`. Pure assembler `buildUsageSummary` (`packages/db-schema/src/entitlements/usage-summary.ts`, 6 Vitest cases): stock/flow from org-summed current-period `usage_rollups` (absent → 0), creation caps from live counts, meters not measurable in web scope → `used:null` (never a misleading 0). DI handler `buildUsageResponse` (`apps/web/src/lib/entitlements/usage-endpoint.ts`, 2 Vitest cases; null resolution → empty payload, short-circuits reads). Live counts today: Spaces + Seats (accepted members); bases/documents/reports (per-Space engine DB) surface limit-only. **REMAINING:** Space-scope payload + period/state fields; destinations/bases/documents/reports live counts (engine round-trip); route-level integration test._
- [ ] 9.2 Wire Space-dashboard utilization placeholders to the endpoints (loading states per §4.5; warning-state treatment)
- [ ] 9.3 Settings → Usage page: org-wide meter list + per-Space breakdown from the endpoints (structure per the paired ui-only `usage-and-billing` design; promotion via /ui-sync flow)
- [ ] 9.4 End-to-end smoke: seeded plan → legacy signup shows offer → subscribe with coupon → run backup → usage appears → cross 90% → single warning email → cross 100% → enforcement holds next run → buy add-on in-app → resumes. Documented as the Verification demo
- [ ] 9.5 Typecheck, build, `db:check`, full test suites green across web/server/workflows/db-schema
- [x] 9.6 Docs: added `pricing-guide.md` §9.1 "Runtime source of truth" — `plan_features`/`resolveEntitlements` is the runtime source (Stripe = money + identity + `plan_slug` only), Features §5.5 flagged superseded for the queued reconciliation, gate-on-slugs rule, the 2.3 catalog-gap blocker recorded, and cross-refs to `admin-entitlements` + ui-only `usage-and-billing`.
