# system-db-schema

Initial implementation spec for `packages/db-schema/` — workspace-package extraction of the master DB Drizzle schema. Renamed from `db-schema` 2026-05 to fit the `system-*` naming convention (structural / cross-cutting; not tied to a single app).
