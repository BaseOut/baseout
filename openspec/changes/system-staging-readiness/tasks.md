# Implementation tasks

> Phases 1–2 are repo-only and land independently (no account access needed). Phase 3 is the single consolidated Dan ask (design D2) and gates Phases 4–5. Per the local-commit loop: implement → surface smoke → human approval → local commit; never push unasked.

## Phase 1 — Repo fixes (no Dan dependency)

- [x] 1.1 `apps/web/src/lib/auth-factory.ts`: add `console.baseout.dev` to `AUTH_BASE_URL.allowedHosts` and `https://console.baseout.dev` to `PROD_TRUSTED_ORIGINS` (and `console.baseout.com` for prod parity). Removes the fallback-to-`baseout.dev` single point of failure (proposal §6). *(2026-09-01: TDD — 2 new tests red→green; consoles pinned in allowedHosts + PROD_TRUSTED_ORIGINS; 19/19 auth-factory tests pass.)*
- [x] 1.2 `apps/web/wrangler.jsonc`: add `"remote": true` to `env.dev` `BACKUP_ENGINE` service binding (matches admin/api and web's own comment). *(2026-09-01: added; matches admin/api and dev.mjs strips it for LOCAL mode as designed.)*
- [x] 1.3 `apps/web/wrangler.jsonc`: add `E2E_TEST_TOKEN` to `env.staging.secrets.required` (design D4). Note the security-review point in the commit body. *(2026-09-01: added with justification comment; secrets checker taught a documented STAGING_ONLY_SECRETS exception — prod deliberately carries neither var nor secret.)*
- [x] 1.4 ~~Untrack `apps/workflows/.env`~~ *(2026-09-01: audit finding was FALSE — `git ls-files` returns nothing, `git log --all` shows no history on any branch, and `git check-ignore` confirms `apps/workflows/.gitignore:30` covers it. The file exists only on disk (and holds a real dev `INTERNAL_TOKEN`, correctly untracked). No action needed; no leak to remediate.)*
- [x] 1.5 Guardrails: add root `"secrets:check": "node scripts/check-wrangler-secrets.mjs"` npm script; wire it into `.github/workflows/ci.yml`; add `staging` to the CI `on.push.branches` list; fix `apps/server/scripts/check-cron-config.mjs` to parse the committed `wrangler.jsonc` across all three envs against `dispatch.ts`, or fold that assertion into the secrets checker and delete it (whichever is the smaller diff, design D3). *(2026-09-01: root `secrets:check` + `cron:check` scripts added; both wired into ci.yml; CI now triggers on staging pushes; check-cron-config.mjs rewritten to parse committed wrangler.jsonc across all 3 envs — chose fix over delete, both checkers green.)*
- [ ] 1.6 Commit the pending `apps/server/.gitignore` `wrangler.local.jsonc` line (isolated from the unrelated WIP threads — stage files by name).
- [x] 1.7 Checks: `pnpm typecheck` + affected builds green; `node scripts/check-wrangler-secrets.mjs` green; no stray `console.*` in the diff. *(2026-09-01: secrets:check + cron:check green; auth-factory tests 19/19; astro check error set byte-identical with diff stashed (75 pre-existing, 0 introduced); no stray console in diff outside CLI scripts.)*

## Phase 2 — Docs reconciled to reality

- [x] 2.1 `shared/internal/ops-setup.md`: rewrite §1 staging + §3 GitHub sections to the actual model — committed `wrangler.jsonc` (no `.example`, no placeholders), Workers Builds deploys with `CLOUDFLARE_ENV=<env>` build commands, staging worker names pinned bare (`baseout-web`/`baseout-server`), migrations via `pnpm db:migrate:tunnel` in the web build command, the six build-time tunnel vars. Mark dashboard facts we can't see as ❓ pending Dan (design D5). *(2026-09-01: top banner + §1 Staging + worker-name table + §3 rewritten to Workers Builds model; dashboard facts marked ❓.)*
- [x] 2.2 `shared/internal/cloudflare-env-separation.md`: add a dated superseded banner pointing at the 3-env model (don't rewrite history). *(2026-09-01: dated superseded banner added.)*
- [x] 2.3 `apps/web/.env.example`: remove the dead `RESEND_API_KEY` block (email = `send_email` binding). *(2026-09-01: RESEND block replaced with send_email-binding note.)*
- [x] 2.4 Fix the stale header comments in the seven wrangler files that still claim `env.staging = baseout-<app>-staging` names, and correct the twelve "pnpm secrets:check … CI fails on drift" comments to name the now-real script (after 1.5). *(2026-09-01: 7 header comments fixed to pinned-bare names; server DO-namespace comment fixed; the 12 secrets:check comments became TRUE via 1.5 — left as-is.)*
- [x] 2.5 `oauth-setup.md`: add/verify a §3 row set for the staging origin `https://console.baseout.dev` per provider (Airtable login + connect, Google Drive, Box, Dropbox, Microsoft) — cells start ❓/❌; flipping them is Phase 3/4 output. (§3.7: read before, update same-change.) *(2026-09-01: §3 re-basing note (console.baseout.dev = staging, Access wall); console rows added for Dropbox/OneDrive/Airtable-Login; §4 preview-framing note marked superseded.)*

## Phase 3 — The consolidated Dan ask (single message; verify → set → report)

- [ ] 3.1 Draft and send the ask, covering (exact values in the message, secrets as parity constraints per design D2):
  1. **Build commands (READ FIRST, design D1)**: paste current Workers Builds build command + root directory for `baseout-web` and `baseout-server` staging (and prod, read-only). Required: web staging = `pnpm db:migrate:tunnel && pnpm --filter @baseout/web build:staging` + deploy of the built artifact; server staging must build with `CLOUDFLARE_ENV=staging`. Watch paths must include `db/**` for web.
  2. **Build vars (web staging pipeline)**: `DB_TUNNEL_HOSTNAME`, `CF_CLIENT_ID`, `CF_CLIENT_SECRET`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` set; Zero Trust service token exists for the tunnel; `CREATE SCHEMA IF NOT EXISTS baseout;` run once on the staging DB; confirm `drizzle.__drizzle_migrations` row count = 40.
  3. **Email**: verify the Email Routing sending domain for `mail.baseout.dev` (DKIM/SPF) on the dev/staging account; list/confirm verified destination addresses for the `EMAIL` binding or confirm unrestricted; confirm `login@mail.baseout.dev` may be shared with prod or give the staging-specific from-address to put in `env.staging.vars.EMAIL_FROM`.
  4. **Secret parity (staging)**: `BASEOUT_ENCRYPTION_KEY` byte-identical web↔server; `INTERNAL_TOKEN` (server) ≡ `BACKUP_ENGINE_INTERNAL_TOKEN` (web, admin) ≡ Trigger.dev Staging `INTERNAL_TOKEN`; `AIRTABLE_OAUTH_CLIENT_ID`/`_SECRET` identical web↔server; full 16-name web + 13-name server `secrets.required` lists confirmed set (names in the wrangler files); `E2E_TEST_TOKEN` set on web staging (new, task 1.3).
  5. **Trigger.dev** (project `proj_lklmptmrmrkeaszrmhcs`): confirm a **Staging** environment exists; its key is what's in `baseout-server` staging `TRIGGER_SECRET_KEY`; Staging env vars set: `BACKUP_ENGINE_URL` (staging engine origin), `INTERNAL_TOKEN`, `ANTHROPIC_API_KEY`, `PUBLIC_APP_URL=https://console.baseout.dev`, plus the four `R2_*` per `shared-managed-r2-staging` 3.1–3.2; confirm who runs `npx trigger.dev@4.5.7 deploy` for Staging (no CI job exists — decide owner or approve a GitHub Actions job as follow-up).
  6. **OAuth redirect URIs**: register the `https://console.baseout.dev` callbacks per `oauth-setup.md` §2 path table for Airtable (login + connect) at minimum; report per-provider status for the §3 matrix.
  7. **Observability** (question, not dashboard work): persisted Workers Logs are OFF in the committed configs — web/admin explicitly (`observability.enabled: false`), server/api/hooks/sql by omission — so NOBODY has stored logs, Dan included; this is a repo fact, separate from our lack of dashboard access. Ask whether that's deliberate (cost) or leftover; if leftover, WE flip it in wrangler.jsonc ourselves (ships with next deploy). Our own permanent visibility channel stays DB-side telemetry (`service_runs`, `backup_runs`) since we will never have dashboard access; Dan is only needed live for coordinated `wrangler tail` moments.
  8. **Local dev DB path**: the old dev DB is gone — provide the tunnel hostname + a service token (or a dev `DATABASE_URL`) we may use locally per `db/scripts/migrate.mjs`'s tunnel mode.
  9. **Cloudflare Access wall** *(verified live 2026-09-01: `console.baseout.dev` 302s everything — including `POST /api/auth/sign-in/magic-link` — to `staging-338.cloudflareaccess.com`)*: grant Access to our tester emails; confirm whether the emailed magic-link verify URL is expected to work only after an Access session; confirm whether the Playwright E2E lane (`E2E_TEST_MODE`) has an Access service-token bypass; confirm `hooks.baseout.dev` and whatever URL Trigger.dev's `BACKUP_ENGINE_URL` points at are NOT behind Access (Airtable webhooks and task callbacks cannot pass an Access login).
- [ ] 3.2 On each reply: record verified facts into `ops-setup.md` / `oauth-setup.md` / `r2-setup.md` cells with date + "per Dan" (design D5); update `env.staging.vars.EMAIL_FROM` if item 3 changes it.

## Phase 4 — Staging verification (gated on Phase 3)

- [ ] 4.1 Magic-link E2E on `https://console.baseout.dev`: request link → email arrives → link lands on `console.baseout.dev` → session established. If it 500s, capture the Worker log line before re-triaging.
- [ ] 4.2 Cron health: read recent `service_runs` rows for `oauth-refresh-sweep` / `run-reconciliation` (via admin surface or tunnel psql); confirm the `*/15` sweep writes `succeeded` rows and a due connection actually refreshes (`access_token_expires_at` advances, no `refresh_claim_unavailable` loop).
- [ ] 4.3 Airtable Connect round-trip on staging (exercises redirect URI + encryption-key parity end-to-end: connect, then wait one sweep and confirm the token row re-encrypts/refreshes cleanly).
- [ ] 4.4 Backup smoke: hand off to `shared-managed-r2-staging` Phase 4 (do not duplicate the §5 protocol here); confirm its precondition list is fully green from this change's Phase 3.
- [ ] 4.5 Log every outcome with date + evidence in this file; failures become follow-up bugs, not scope creep.

## Phase 5 — Close out

- [ ] 5.1 Update `lat.md` graph with the load-bearing facts that changed (build-time env flattening, three-names-one-token contract, cron→job map location); run `lat check`.
- [ ] 5.2 Human-approved local commits per thread (never mix with the queue-serialization or R2 WIP threads); no push without approval.
