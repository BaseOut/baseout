# Tasks

## 1. better-auth config + schema

- [x] 1.1 `auth-factory.ts`: `twoFactor` plugin (TOTP, backup codes, trusted device, rate limiting) + TOTP-secret encryption hook using the master key. → `twoFactor({ issuer:'Baseout', allowPasswordless:true, trustDeviceMaxAge:30d, backupCodeOptions:{amount:10} })` + built-in rateLimit customRules (verify endpoints 5/min); master-key layer via adapter storage hook `src/lib/two-factor/encryption.ts` (AES-256-GCM under BASEOUT_ENCRYPTION_KEY on top of the plugin's own encryption — better-auth 1.6.9 has no per-field storage hook, so the adapter boundary IS the hook point).
- [x] 1.2 Migration via better-auth schema generation (twoFactor table); `db:check` clean. No password columns. → `apps/web/drizzle/0032_two_factor.sql` (`two_factors` table + hand-added `users.two_factor_enabled` — canonical users drizzle table lives in @baseout/db-schema which this change may not touch; adapter maps it via `src/lib/two-factor/adapter-schema.ts` mirror). NO password columns. `db:check` correctly reports 0031/0032 as PENDING — generated only, human applies at smoke time per instructions.

## 2. Flows (TDD — auth flows are §3.4 mandatory-test surface)

- [x] 2.1 Enrollment (verify-to-activate) + backup-code issuance; audit rows + notification emails. → plugin-native verify-to-activate (`verified=false` until first verify-totp; unverified enrollments never challenge). Audit rows (`auth_audit_log` kinds `2fa_enroll_started/enabled/disabled/backup_code_consumed/challenge_failed`) + notification emails (`templates/two-factor.ts`) via `src/lib/two-factor/events.ts`, fed by the adapter activation hook + response-observation hooks in `all-methods.ts`. Deviation: audit metadata does not carry the REMAINING backup-code count (not exposed at the adapter boundary without decrypting with the BA secret).
- [x] 2.2 Challenge interception for magic link AND SSO; trusted device; backup-code consumption; disable-requires-factor. → stock plugin only intercepts credential sign-ins, so `src/lib/two-factor/all-methods.ts` extends the identical semantics to `/magic-link/verify` + `/oauth2/callback/:providerId` (revoke fresh session, arm the plugin's `two_factor` cookie, redirect to `/2fa`); trust-device verify/rotate replicates the plugin's HMAC byte-for-byte (known-vector test). Minimal functional `/2fa` challenge page (`src/pages/2fa.astro`, public path) using existing TextInput/Checkbox/Button + setButtonLoading — full design stays in ui-only `login-methods`. Disable-requires-factor: better-auth 1.6.9's `allowPasswordless` disable endpoint waives proof for credential-less users, so a custom BEFORE-hook on `/two-factor/disable` demands a current TOTP code or unspent backup code, verified against the plugin's own stored material (interop TOTP in `src/lib/two-factor/totp.ts`, RFC 6238 vector-tested).
- [x] 2.3 CSRF via better-auth helpers on enrollment/disable; no `console.*`. → all 2FA mutations go through better-auth's own `/api/auth/two-factor/*` endpoints (origin-check CSRF via trustedOrigins); no raw POST handlers added; grep clean of `console.*`/`debugger` in new files.

## 3. Verification

- [x] 3.1 Vitest + integration suites green; `typecheck` + `build`. → 35 tests green across `src/lib/two-factor/*` (encryption wrapper, interception helpers + HMAC known vector, RFC 6238 TOTP vectors) + `auth-factory.test.ts` (plugin registration, endpoints exist on a real betterAuth instance, rate-limit rules, no-password guard). `astro check`: no NEW errors (6 pre-existing in tests/integration/airtable-persist.test.ts).
- [ ] 3.2 E2E (Playwright): magic-link + challenge; backup-code recovery; trusted-device skip. → NOT DONE: needs an enrolled user + authenticator secret fixture and the applied 0032 migration; deferred to human smoke (page: /2fa; endpoints: /api/auth/two-factor/*).
- [x] 3.3 Cross-check challenge hand-off with `web-auth-airtable-sso`; UI states with ui-only `login-methods`. → the interception hook matches `/oauth2/callback/:providerId` (the genericOAuth callback path registered by web-auth-airtable-sso), pinned by `INTERCEPTED_SIGN_IN_PATHS` test; UI-state cross-check with ui-only deferred with the screens themselves (only the minimal /2fa page ships here).
