# Implementation tasks

## 1. Incremental-backup task

- [x] 1.1 New `apps/workflows/trigger/tasks/incremental-backup.task.ts` — thin wrapper; pure module `incremental-backup.ts` taking injected deps. Payload: `{ runId, spaceId, subscriptionId, baseId, connectionId, cursor, reconcile }`. Opens the pass as `bo_at_base_runs` with `run_type='incremental'`. → Done. Pure module + wrapper per CLAUDE.md §6; `db.openBaseRun({ runType: 'incremental' })` at pass start. The engine-brokered per-space transport + payloads auth (webhook id ach…, decrypted token, `lastReconciledAt` anchor) are stubbed `notYetWired` in the wrapper with a TODO referencing server-instant-webhook Phase D — invoking them fails loudly and posts a failed completion. → UPDATE 2026-07-23: stubs REPLACED — `createIncrementalDbTransport` (op-dispatch onto the engine's `/spaces/:spaceId/incremental-apply`, adding the wrapper's `baseId` to schema/record/version/table-scope ops) + `fetchSubscriptionContext` (`/webhook-subscriptions/:id/context`, server-dynamic-mode 4.1) now wire the real deps; the wrapper builds the Airtable surface (`fetchPayloadsPage` + shared client meta/records) from the resolved token and threads `lastReconciledAt`. Context-resolution failure (409 token_unavailable) throws → failed completion → next poll tick retries.
- [x] 1.2 `_lib/airtable-payloads.ts` — payloads client: `GET /v0/bases/:baseId/webhooks/:webhookId/payloads?cursor=` loop (≤50/page) while `mightHaveMore`; tolerant typed parsing (all change maps optional; unknown keys logged); error-payload variant (`INVALID_HOOK`/`INVALID_FILTERS`/`INTERNAL_ERROR`); cursor-expiry detection. → Done. `fetchPayloadsPage` (one page; the pure module loops) + `parsePayloadsResponse` (unknown keys → structured `log`, never fatal; malformed body throws). Cursor expiry = 4xx whose error body mentions the cursor being invalid/expired → `PayloadsCursorExpiredError` (heuristic documented in-module — Airtable publishes no stable machine code; verify against the live API when Phase D wires auth).
- [x] 1.3 Schema-application module (per payload, before records): created/destroyed tables + fields → lifecycle (destroys = confident `removed` + cascade); `changedFieldsById`/`changedMetadata` → entity updates + `bo_at_schema_updates` with before/after + `breaks_data`; description writes touch only `description`; `changedViewsById` gated on Enterprise view capture. → Done. `computeSchemaWrites` emits a typed `SchemaWrite` union; destroys carry `status:'removed', cascade:true` (never `unknown`); retypes log `breaks_data:true` with payload previous/current as before/after; description patches carry only the `description` key; views skipped unless `viewCaptureEnabled`.
- [x] 1.4 Record-application module: `createdRecordsById` → records + sparse rfd (no first-population log); `changedRecordsById` → superseded-value log from stored rfd value, value-equality idempotency guard, drift counting vs payload `previous`; `destroyedRecordIds` → `status='deleted'`. `unchanged` values sampled for drift only. → Done. `computeRecordWrites`/`diffChangedRecord` read stored cells via the injected `getStoredRecords` seam; `oldValue` always from OUR stored value (payload `previous` used only for drift); equal values skip write AND log (idempotent replays); no-rfd-row cells create with `log: null`; `unchanged` compared for drift, never applied.
- [x] 1.5 Attribution: thread `actionMetadata` → `action_source` + `actor` onto every payload-derived schema/record update row. → Done. `extractAttribution`: `action_source` = `actionMetadata.source`; `actor` = user `email ?? name ?? id` (both columns are `text` in space schema v8). Reconciliation writes carry explicit NULL attribution.
- [x] 1.6 End-of-pass (schema events only): meta-API full-schema fetch → hash-deduped `bo_at_schema_versions` insert + verify vs applied state (mismatch → `reconcile=true`); regenerate affected per-table views / refresh matviews. → Done. `schemaHashOf` (key-order-stable sha256) + `insertSchemaVersion`; verification compares table/field id sets + field types via `getAppliedSchemaState`; `regenerateViews(affectedTableIds)`. Record-only passes make zero extra Airtable calls (test-pinned).
- [x] 1.7 Cursor callback after each durably-applied batch: POST `/api/internal/webhook-subscriptions/:id/cursor`. → Done. Posted per page after all its payloads apply (skipped when the cursor didn't move); `createEngineCallbacks` treats the server's monotonic-guard 409 as success (idempotent replays), throws on other non-2xx so the task retries.
- [x] 1.8 Reconciliation pass: trigger on `reconcile=true` | drift threshold | verification mismatch | table created this pass. `LAST_MODIFIED_TIME() > last_reconciled_at` paging (extend `_lib/airtable-client.ts` if needed) + record-ID deletion sweep; `reconciled_records` counted separately; no attribution on reconciliation writes. → Done. All four triggers wired (drift threshold defaults 0 — any drift reconciles); `IS_AFTER(LAST_MODIFIED_TIME(), "<anchor>")` paging with cleared-cell detection; created-this-pass tables page unfiltered (full fill); fields-free id sweep marks stored-but-absent rows `deleted`; sweep deletions + upserts count into `reconciled_records` only. `airtable-client.ts` `ListRecordsOptions` extended with `filterByFormula` / `fields[]` / `returnFieldsByFieldId`.
- [x] 1.9 Fallback: `INVALID_HOOK`/`INVALID_FILTERS` or cursor-expiry → POST `/api/internal/webhook-subscriptions/:id/fallback` with reason, exit `{ status: 'fallback_to_full' }`; `INTERNAL_ERROR` → skip transaction + `reconcile=true`. → Done. Fallback exits without advancing the cursor past the failing batch; cursor expiry aborts before any application; the base run is completed `failed` with `fallback_to_full:<reason>` so it never dangles.
- [x] 1.10 Engine progress/completion callbacks with created/updated/deleted/reconciled counts; all Airtable calls through the shared client + per-Connection gateway. → Done for completion: wrapper POSTs `/api/internal/runs/:runId/complete` (fire-and-forget, mirrors backup-base) with created/updated/deleted/reconciledRecords + driftCount + finalCursor. No per-batch progress callback — incremental passes are short (cleanup-expired-snapshots precedent); add one if run-detail UX needs it. Gateway routing is part of the Phase D wiring TODO (the pure module's Airtable surface is fully injected, so the gateway slots into the wrapper without touching orchestration).

## 2. Tests

- [x] 2.1 `apps/workflows/tests/incremental-backup.test.ts` — fixture payload streams: happy path (cursor per batch); schema-before-records ordering (field created + populated in one transaction); destroys (table/field/record) → confident `removed` + cascade; superseded-value log uses stored rfd value; drift seed (payload `previous` ≠ stored) → counter + reconcile flip; attribution threading; multi-page `mightHaveMore`; mid-batch retry idempotency; created-table partial fill → forced reconcile; error payloads (all three codes); cursor-expiry → fallback; record-only pass makes no meta call; schema pass regenerates views. → Done — all listed behaviors covered (35 tests, TDD red→green), plus transaction-number ordering, description-only patch shape, view-capture gating, payload-previous-agrees no-drift, and tolerant-parse/unknown-key cases.
- [x] 2.2 Reconciliation: catches seeded payload-stream miss; deletion sweep; `reconciled_records` reporting. → Done — seeded source≠DB miss applied with NULL attribution, sweep-deletes a source-absent record, `reconciledRecords` counts both; created-table full-fill picks up a record the payload's partial `recordsById` missed.
- [x] 2.3 Task wrapper test: payload → pure-module deps adaptation. → Done via the repo's wrapper-test convention (restore-base-task style: exercise the exact transport the wrapper wires, not the SDK task object): `createEngineCallbacks` URL/token/body shapes, 409-as-success, non-2xx throw, fallback POST; plus the airtable-client reconciliation-options forwarding the wrapper's Phase-D wiring will rely on.

## 3. Verification

- [x] 3.1 `pnpm --filter @baseout/workflows typecheck && pnpm --filter @baseout/workflows test` — green. → Done 2026-07-23: typecheck clean; 30 files / 267 tests passing (232 pre-existing + 35 new; cleanup suite intact at 13). No `console.*` in shipped code (structured `log` dep → `logger.info` in the wrapper). → Re-run 2026-07-23 after the Phase-D transport wiring (task 1.1 update): typecheck clean; 30 files / 273 tests (+6 transport-mapping tests: context resolution URL/token/mapping + 409-throw; db-transport op bodies incl. baseId injection, read-seam unwrapping, non-2xx op+status throw).

## Open follow-ups (post transport wiring, 2026-07-23)

- [ ] 4.1 Per-Connection gateway routing for payload polls + reconciliation paging (shared 5 rps per-base budget with snapshot backups) — polls currently hit Airtable directly with the shared client's own 429 backoff; flagged in the wrapper header.
- [ ] 4.2 Widen the engine's `/api/internal/runs/:runId/complete` body contract: the incremental completion POSTs `{created, updated, deleted, reconciledRecords, driftCount, finalCursor, status}` but the route's parseBody still requires `tablesProcessed/recordsProcessed/attachmentsProcessed` (and `fallback_to_full` isn't an accepted status) → the completion currently 400s and incremental runs terminate via the reconciliation sweep instead. Cross-app contract change (engine route + this wrapper) — flagged since server-instant-webhook D.3.
