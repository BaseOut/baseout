{
  "name": "baseout",
  "private": true,
  "version": "0.0.0",
  "description": "Baseout monorepo — backup, restore, and data intelligence layer for Airtable",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "postinstall": "node scripts/fix-symlinks.js",
    "fix:symlinks": "node scripts/fix-symlinks.js",
    "build": "pnpm -r --parallel run build",
    "test": "pnpm -r --parallel run test",
    "typecheck": "pnpm -r --parallel run typecheck",
    "lint": "pnpm -r --parallel run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml,astro}\"",
    "dev:web": "pnpm --filter @baseout/web dev",
    "dev:server": "pnpm --filter @baseout/server dev",
    "dev:admin": "pnpm --filter @baseout/admin dev",
    "dev:api": "pnpm --filter @baseout/api dev",
    "dev:sql": "pnpm --filter @baseout/sql dev",
    "dev:hooks": "pnpm --filter @baseout/hooks dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^10.2.1",
    "lat.md": "^0.11.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.59.1"
  },
  "pnpm": {
    "comment": "overrides pin transitive deps to their first patched version to clear Dependabot high CVEs (system-dep-remediation batch 2b). Same-major only. Excluded: undici (wrangler 3.x/miniflare — wrangler 3→4 upgrade follow-up), drizzle-orm (db-schema 0.36→0.45 migration follow-up), react-router (fix 7.18.0 younger than the 7-day minimumReleaseAge gate — defer), sharp (astro-pinned — verify compat separately).",
    "overrides": {
      "fast-uri": "^3.1.4",
      "ip-address": "^10.3.1",
      "hono": "^4.12.25",
      "svgo": "^4.0.2",
      "brace-expansion": "^5.0.7",
      "ws@8.18.0": "^8.21.0"
    }
  }
}
