// Structured error types — typed errors that serialize to consistent JSON
// responses across all apps. Includes upgrade-hint shape for 402 responses,
// rate-limit-hint shape for 429, etc.
// Implementation pending.

export {};
