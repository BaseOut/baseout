// HMAC service-token issuer + validator for cross-app calls.
// Used by every cross-service call (web → server, api → server,
// hooks → server, admin → server).
// Implementation pending.

export {};
