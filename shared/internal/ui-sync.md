# ui-only Sync — Ledger & Mechanism

Owner: whoever runs a sync. **Update this file in the SAME change as every
sync or promotion** (the CLAUDE.md §3.7 discipline, same as `oauth-setup.md`
and `r2-setup.md`). The procedure lives in `.claude/skills/ui-sync/SKILL.md`
(invocable `/ui-sync`); this file holds the mutable state.

---

## 1. Remote & mechanism

- Remote: `ui-only` → `git@github.com:BaseOut/ui-only.git` (Dan's design
  fork; a full monorepo fork with **unrelated git history**).
- **NEVER merge.** The mechanism is `git fetch ui-only` + selective file
  copy at a pinned `ui-only/main` hash (`git show ui-only/main:<path>` /
  `git checkout ui-only/main -- <path>` + relocate). One sync = one commit
  whose subject cites `ui-only@<hash>` — `scripts/ui-sync-status.mjs` parses
  that convention to find the last-synced point.
- Status: `pnpm ui:sync-status` (pending commits, changed files by surface,
  dirty-target warnings).
- **Reverse & drift:** `pnpm ui:sync-drift` classifies every synced surface as
  `in-sync` / `forward-pending` (fork ahead) / `reverse-pending` (WE are ahead,
  `apps/design/**` only) / `diverged` (both). The `/sync-reconcile` skill drives
  the reverse path — it emits a human-applied patch UP to `ui-only` and never
  auto-pushes. `--path <prefix>` scopes the report to one surface. Content-level
  3-way (byte-compare modulo import rewrites) is a follow-up; v1 is file-level.
- **Ledger gate:** `pnpm ui:sync-check` fails when a `chore(design): sync
  ui-only@…` tip commit did not also update THIS file (§6 rule, enforced).
- **storybook.ts reconcile:** `pnpm storybook:reconcile` prints the fork-side and
  local-side diffs of the shared catalog for the §2 standing 3-way merge.

## 2. Path-mapping table

| ui-only path | lands at | treatment |
|---|---|---|
| `apps/design/**` | same path | verbatim (pages/fixtures get the `@web` rewrites below; `src/pages/prototypes/*.html.ts` get the research-depth rewrite) |
| `apps/web/src/views/**` | `apps/design/src/views/**` | verbatim copy (files use relative imports; no rewrites needed) |
| `apps/web/src/components/{schema,ui}/**` | `apps/design/src/components/{schema,ui}/**` | verbatim copy (relative imports carry over) |
| `apps/web/src/components/integrations/**` | `apps/design/src/components/integrations/**` | verbatim copy (added 2026-07-29 — same localization convention as schema/ui) |
| `apps/web/src/stores/**` | `apps/design/src/stores/**` | verbatim copy (added 2026-07-29) — the design harness compiles against the FORK's store types; reconcile into the real `apps/web/src/stores/*` at promotion (fork type model ≠ persisted model until then) |
| `apps/web/src/components/layout/**` + `apps/web/src/layouts/**` | **straight to `apps/web`** at promotion — NO design copy | the design harness renders the app shell via the `@web` alias (`apps/design/src/layouts/SidebarLayout.astro` wraps `@web/layouts/SidebarLayout.astro`), so shell changes only render once they land in apps/web |
| `apps/web/src/lib/**` (design-coupled helpers, e.g. `csv.ts`) | `apps/design/src/lib/**` | verbatim copy; `ui.ts` stays a shim |
| `apps/web/src/styles/**` | **promotion-time only** → `apps/web/src/styles/**` | design has no style copies — `design.css` @source-scans apps/web, so styles reach the harness once promoted |
| `apps/web/app-config.json` | **never-sync** | fork drift (upstream carries routes we don't have); promotion adds entries selectively |
| `overview/**` | `apps/design/overview/**` | verbatim |
| `research/**` (HTML/MD) | `apps/design/research/**` | verbatim; **`shots/*.png` skipped** — retrievable at the pinned hash. Prototype pages import these via `?raw`: rewrite `../../../../../research/` → `../../../research/` |
| `openspec/changes/<x>/` | `openspec/changes/web-<x>/` (or per §3.6) | imported, task state preserved, backend-blocked tasks annotated |

**Permitted `@web` rewrites (design pages/fixtures only):**
`@web/components/schema` → `../components/schema`, `@web/views/SchemaView.astro`
→ `../views/SchemaView.astro`. All other `@web/*` imports (layouts, `lib/config`)
stay — they intentionally resolve to the real apps/web via the alias in
`apps/design/astro.config.mjs`.

**Permitted rewrites on sync (the ONLY edits):** `@web/views` → relative,
`@web/components/schema` → local, `@web/lib/ui` → local shim. Everything
else stays byte-identical so the next sync diffs cleanly.

**Standing exception:** `apps/design/src/lib/storybook.ts` — both sides edit
it; every sync is a 3-way reconcile (keep local entries + take upstream
entries). Note the reconcile in the ledger row.

**Never-sync list:** `.claude/**` (fork hooks/settings, e.g.
`ds-guard.mjs`), root `package.json` + lockfile, CI config, fork-local asset
deletions (e.g. `apps/web/public/images/auth-bg.png` removal), binary
screenshots under `research/**/shots/`.

## 3. Sync ledger

| date | ui-only hash | baseout commit | scope |
|---|---|---|---|
| 2026-06-12 | `f0f9171` | `789727e` | initial fork integration: apps/design baseline + openspec imports |
| 2026-07-04 | `beb43a7` | `6d4c698` | decoupled round-2 handoff surfaces only (flow-registry, handoff, schema-nav, planning docs); schema web layer deferred |
| 2026-07-08 | `d97c777` | `53110f8` | round-2/3 schema web layer LOCALIZED into apps/design (21 schema components + SchemaView + Drawer); React enabled; storybook 3-way |
| 2026-07-10 | `3153dfd` | `e93f111` | schema export control + csv.ts, panels round-4 (EntityPanel anchor model, entityChip/locationCrumbs), drawer/changelog/facet polish, prototypes pages, research+overview docs, `web-notifications-inbox` spec import. storybook.ts merged as upstream-skeleton + 11 local-only entries (upstream ships a duplicate `tooltip` id — left verbatim). Deferred to the 4c promotion commit: `fixtures/inbox.ts` + the design `SidebarLayout.astro` wrapper (both type-import `@web/components/layout/inbox`, which exists only after the app-shell promotion) |
| 2026-08-13 | `7c7202d7` | (web-sources) | SCOPED promotion — account-level Sources surface only: `views/{SourcesView,SourceAddView,SourceDetailView}` promoted straight into `apps/web` (verbatim; one strict-tsc `connecting` cast in the detail view) + promoted-alongside deps `lib/registry/removal.ts`, `components/integrations/AccessScopeNote.astro` (scoped style → global.css), `lib/ui.ts` `CONFIRM_DESTRUCTIVE`, `ConfirmModal` `value="confirm"`, and the shared `.tbl-frame`/`.page-head`/`.sch-slot(-in)`/`.sch-cap-mark`/`.sch-modeswitch`/`.asn*` global classes. Data wired to REAL Airtable-connection data (`getIntegrationsState → toSourceSummary`); the one new prop `accessLostAt` reads `connections.invalidatedAt ?? pendingReauthAt`, `addedAt` reads `createdAt`, `lastChecked` honestly gated. Not synced to apps/design (promoted direct to web — the harness already carries these fork views' family). |
| 2026-08-13 | `7c7202d7` | (web-destinations) | SCOPED promotion — account-level Destinations surface (twin of web-sources): `views/{DestinationsView,DestinationAddView,DestinationDetailView}` promoted VERBATIM into `apps/web` (no strict-tsc cast — `DestinationStatus` already carries all 4 states). ZERO new promoted-alongside deps (all landed with web-sources: `lib/registry/removal` already handled `kind:'destination'`, `CONFIRM_DESTRUCTIVE`, `ConfirmModal value="confirm"`, `schema/tableSort`, shared `.tbl-frame/.page-head/.sch-*` globals). Data wired to REAL per-Space storage destinations (`getIntegrationsState → toDestinationSummaries`) — every prop already populated, so NO store/mapper/schema change; pages trimmed to fork props (`detail.astro` already matched). **Swap-primary + provider-connect grid DROPPED from the account registry view** (stay on the per-Space Backups config / `shared-multi-destinations`); `api/connections/storage/*` contract untouched. Not synced to apps/design (promoted direct to web — harness already carries the fork family). |
| 2026-08-13 | `7c7202d7` | (web-space-home-dashboard) | SCOPED promotion — Space Home landing: the fork's grown `views/SpaceHomeView.astro` (28KB→57KB) promoted into `apps/web` (old→`.legacy.astro`), reconciled to REAL wiring — Props contract is IDENTICAL to the pre-promotion view, so `pages/index.astro` (feed via `listRecentRuns` + `getIntegrationsState` + `toSourceSummary`/`toDestinationSummaries`) is UNCHANGED, including its page-level 2s `data-backup-history`/home-live poll. New dep: the `deriveSpaceHealth`/`isInFlightRun`/`runKind`/`runMovedAt` health derivation GRAFTED into `lib/backups/format.ts` (additive — no current export dropped; `formatNextScheduledAt` from `lib/time` aliased to avoid colliding with format.ts's own single-arg export) **+ tests** (fork shipped it untested). Three reconciliations vs fork-verbatim: (1) `SpacePipelineHero` imported from `components/patterns/` (cataloged + story) not the fork's `components/integrations/`, and widened to nullable `spaceName`; (2) `RunBackupNowModal` keeps apps/web's REAL `spaceId`+`redirectTo` POST contract, not the fork's navigation-only `confirmHref`; (3) `.hm-kpi*` metric-strip rules stay scoped in the view (apps/web has no `pattern-metric-tiles` global + single call site — §1.5). `data-narrow-pan`/`data-m` are inert-but-harmless (no `panRail.ts`/`pattern-mobile-row` in web; scoped `@media` handles mobile, `.hm-tablewrap` overflow-x retains scroll — panRail gated like web-sources). `<LiveRefresh>` reused as-is (already promoted). **DashboardView RETIRED → `.legacy.astro`** (dead code — imported nowhere; fork deleted it outright). NOT synced through apps/design. Deferred: the fork's `SidebarLayout` connection-banner reading the same `deriveSpaceHealth` (separate app-shell surface — kept atomic). |
| 2026-08-13 | `7c7202d7` | (web-backups-redesign) | SCOPED promotion — the Backups RUN surface (twin family to Sources/Destinations/Space-Home). The fork's grown `views/{BackupsListView 21→35KB, BackupRunDetailView 26→51KB, BackupRunBaseView 22→26KB}` promoted into `apps/web` (old→`.legacy.astro`), reconciled to REAL data. **FacetFilter DROPPED** (the diverged schema cluster — Airtable field-glyph + workspace-grouping — per the RestoreHistoryView precedent) → search + click-sort + `TablePager` via the shared `createRunLog`; `refineCollapse` dropped with it. `BackupsListView` re-adds `embedded`+`spaceId` so `backups.astro` keeps owning the header (destination line · FrequencyPicker · webhook banner) AND the `[data-backup-history]`/`data-space-id`/`[data-backup-history-state]` poll wrapper (page DOM UNCHANGED); the **live poll is re-grafted via the `home-live` reload-on-transition strategy** (the fork dropped the in-place tbody rebuild) so §5 must-refresh holds. `run.astro` builds the new `RunDetail` shape (`error`/`outcome`/`createdAt`/`queued` from the DB row; per-base from `engine.getRunDetail`; `metricsPending` preserved = run-level totals + a "per-base unavailable" note). All three views use web's REAL `RunBackupNowModal` (`spaceId`+`redirectTo`), not the fork's `confirmHref`. New deps: `components/backups/copyId.ts` + `components/schema/entityIcon.ts` (verbatim), `components/ui/Drawer.astro` (scoped style→global.css + story + `component-classification` entry), `lib/backups/format.ts` `backupRunTitle` (+tests). Two strict-tsc reconciles: `copyId` `root: ParentNode`→`HTMLElement` (workerd-global shadow), `kindMeta` keyed by `string`. **SKIPPED — imported by NO promoted run-view (Integrations/wizard surface only): `BackupHistoryWidget`, `CadencePicker`, `BackupScheduleScope`, `components/backups/RunBackupButton`, `StoragePicker`.** `.hm-kpi*` kept scoped in the detail view (§1.5, 2nd copy after SpaceHome); `data-narrow-pan`/`data-mobile-rows`/`data-m`/`data-nrow` inert-but-harmless (no panRail/pattern-mobile-row in web; scoped `@media` + `overflow-x` handle mobile). NOT synced through apps/design (promoted direct to web). |
| 2026-08-13 | `7c7202d7` | (web-login-methods) | SCOPED promotion — the AUTH surface (forward-pending, NOT diverged). The four fork views promoted straight into `apps/web` (verbatim markup; the fork's FIXTURE `<script>` blocks replaced with the REAL better-auth wiring): `views/LoginView.astro` → `pages/login.astro` (real `authClient.signIn.magicLink`, `returnTo` + `?error` preserved, honestly-gated Airtable SSO button via `POST /api/auth/sign-in/oauth2`), `views/WelcomeView.astro` → `pages/welcome.astro` (real `POST /api/onboarding/complete` + SSR domain-association fork via `resolveOrganizationsForEmail`), `views/AuthChallengeView.astro` → `pages/2fa.astro` (real `verify-totp`/`verify-backup-code` contract, replaced the minimal functional page), `views/AuthAssociationView.astro` mounted SSR-gated on `/welcome` (real `POST /api/onboarding/join-request`). Legacy pages → `views/*.legacy.astro`. **RegisterView SKIPPED** — the fork version is a 602B stub delegating to LoginView; the working `pages/register.astro` stays. NO auth logic/routes touched (`lib/auth*.ts`, `pages/api/auth/*`, `pages/api/onboarding/*`, `pages/api/organizations/join-requests/*`, middleware). Promoted-alongside deps: `ui/CodeInput.astro` (+ story + `code-input` Primitives entry in `apps/design/src/lib/storybook.ts` + classification), `lib/auth/{codeInput,types}.ts`, `styles/components/code-input.css` (+ global.css import), five new `.auth-provider-mark/-note`/`.auth-form-note`/`.auth-field-label`/`.auth-trust` classes in `styles/components/auth.css`. HONEST GATES: Airtable SSO button rendered only when the genericOAuth provider is configured server-side (login app still pending, web-auth-airtable-sso 0.1); `adminName` un-surfaced (no client contract) + only the top matched org offered on the association screen (multi-org = shared-* follow-up); account-security SecurityPanel deferred (separate surface). Not synced through apps/design (promoted direct to web — the harness already carries the fork family). |
| 2026-08-13 | `7c7202d7` | (web-settings) | SCOPED promotion — the unified, catalog-driven SETTINGS hub (forward-pending, NOT diverged). The fork's `views/{SettingsView,settingsCatalog,settingsControls}` promoted into `apps/web` (into `views/`, NOT `components/`, so the classification/story gate that governs `components/**` doesn't apply — only the raw-markup view allowlist), reconciled onto REAL data. FOUR sections wired live: **API tokens** + **AI keys** render as real panels in the Developer pane (`views/{ApiTokensPanel,AiKeysPanel}.astro`, lifted VERBATIM from the pre-hub `pages/settings.astro` + `pages/settings/ai-keys.astro` — create/revoke/rotate scripts + DOM ids + plaintext-once modal UNCHANGED; SSR query/mapping moved to the thin `pages/settings.astro` loader; live `/api/tokens/*` + `/api/ai-keys`, hashes-only, `byo_ai_key` Plus+ gate); **billing** "Open portal" → real `POST /api/billing/portal` (`data-set-portal` handler added to `settingsControls`, `setButtonLoading`), usage → `/reports`; **space** retention/schedule/destination → real `<a href>` to `/retention` (kept live, untouched) `/backups` `/destinations`. Everything with no apps/web route is GATED honestly via a new `SettingsRow.gated` (text→readonly, select/toggle→disabled — no fake "Saved") or a deferred-action note: account name/profile/sessions/delete, org identity/audit, space name/auto-add, billing email/invoices/overage, all notifications, webhook secret, SQL details, and the whole **Security (2FA)** category. `SecurityPanel.astro` + `lib/auth/securityPanel.ts` promoted (imports resolved; `ParentNode`→`Document\|HTMLElement`) but the category is gated off (2FA backend deferred at the Auth promotion → honest note). Fork FIXTURE dropped: the `ENROLLED_WORKSPACES` "invented Spaces" block. DOM-type-shadow reconciles: `settingsControls` `ParentNode`/`Element`→`HTMLElement`, selects queried as `HTMLElement` (not `HTMLSelectElement`) + structural `.value`. `pages/settings/ai-keys.astro` → redirect to `/settings?tab=developer`. Legacy rollbacks: `views/SettingsHub.legacy.astro` + `views/AiKeysView.legacy.astro` (not routed). Not synced through apps/design (promoted direct to web — harness already carries the fork family). |
| 2026-07-29 | `9a8b448` | (this commit) | SCOPED sync — base-picker workspace-grouping surface only: `components/integrations/{BaseSelectionTable,BasePickerRow,basePickerSearch}` + `schema/{EntitySearch,typeaheadItems,airtableGlyph}` + `views/IntegrationsSetupWizard` localized from fork apps/web; design pages `integrations/{configure,authorizing}` + `login.astro`; `fixtures/{integrations,sources,destinations}` + `lib/wsResolve` + `stores/{connections,sources,destinations}` + `components/backups/BackupScheduleScope`; storybook.ts PARTIAL reconcile (took `pattern-base-picker` ONLY — ~1.6k lines of other upstream entries ride the next full sync); `login-methods` spec imported as `web-login-methods`. **DEFERRED (flagged):** comments-explorer (`9a8b448` first slice — churning), schema field↔view cross-refs (`27404bf`+), data-lab/Data page batch, handoff/prototype rounds, remaining ~190 fork commits. |
| 2026-08-14 | `7c7202d7` | (web-data-page) | SCOPED promotion — the NET-NEW Data browse surface (Records · Attachments · Changelog), wired to the EXISTING engine read routes (`server-data-browse` `data/tables/:id/records` + `data/changelog`; `server-media-index` `media[...]`). apps/web had only the Data phase-1 SHELL (SoonTab panels, no `components/data/`), so the fork components are promoted CANONICALLY from the FORK (they're diverged in the design harness; apps/web had none). 31-file closure promoted VERBATIM (DOM-type-shadow reconciles only): `components/data/{DataBrowse,DataChangelog,DataMedia,MediaPanel,MediaThumb,RecordPanel,LockedTab,StaticImport}.astro` + helpers (`dataTypes,mediaFormat,mediaReadBody,recordReadBody,runReadBody,commentText`), `schema/{FacetFilter,locationCrumbs,workspaceGroups}`, `ui/{DateRangePicker,PanelHost,panelHostRegistry,panelAddPicker,panelStack,pickerSearch,popoverPlace,splitView,tooltip,viewState}`, `lib/{listSheet,mobileNav,refineCollapse,refineFacetIcons}`. Reconciles: `ExportControl` import rewritten `../schema/`→`../patterns/` + its usages trimmed to apps/web's ExportControl API (dropped `formats`/`splitScope`/`changeScope`); one DataMedia JSX comment relocated out of a `<tr>` attribute list (astro-check parse); `HTMLSelectElement`→`HTMLInputElement` + nullable-`root`/`tpl` rebinds + `viewState` `ParentNode`→`HTMLElement` (workerd DOM-shadow, RestoreView.legacy pattern); 4 scoped `<style>`→`<style is:global>` (DataMedia/MediaThumb/LockedTab/DateRangePicker — the inbox `is:global` precedent). Engine client + `data/{records,changelog,media,media/totals,media/[assetId],media/[assetId]/download}` proxies added (IDOR + tier via `guardSchemaDocsRequest`; SSR mapping in tested `lib/data-browse/map.ts`). **Records/Changelog/Attachments = REAL; Comments = GATED** (server capture-only, no read route → Phase-4 `server-comments-read`, cross-ref in server-comments) — *Comments went REAL 2026-08-14 via `server-comments-read`, see the row below*; Docs/Chat soon; **managed_pg gate honest** (routes 501 on other backends → "available on managed Postgres Spaces" state). NOT synced through apps/design (promoted direct to web — harness already carries the fork family). |
| 2026-08-14 | `d6c02078` | (web-data-page · Comments read) | SCOPED promotion — the Data ▸ **Comments** tab goes from GATED to REAL. Backend half is the NET-NEW [`server-comments-read`](../../openspec/changes/server-comments-read/) change (bare `server-` prefix): `GET /api/internal/spaces/:id/data/comments` over the `bo_at_comments` table `server-comments` already writes — pure `per-space/comments-read.ts` (keyset cursor + parameterized WHERE/ORDER/keyset + raw→wire mapper) + `comments-read-io.ts` + route, mirroring `data-records`/`data-changelog` (managed_pg-only → same `501 backend_not_implemented`; INTERNAL_TOKEN gate). Web half: `DataComments.astro` promoted from the fork (`d6c02078` "Comments stops crushing the column it is named for") + classified (`daisyui-direct-styleguide`) + storied; `DataView` swaps the gated Comments SoonTab for it behind the same managed_pg honest gate; `data.astro` SSR-loads page-1 (space-wide). New `getDataComments` engine method + `data/comments` proxy (IDOR + tier via `guardSchemaDocsRequest`) + `mapComments` (+ tests). Reconciles vs fork-verbatim: `ExportControl` import `../schema/`→`../patterns/` + dropped its `formats` prop (apps/web ExportControl API); the one scoped `<style>`→`<style is:global>` (its selectors already `.cm`-scoped, so no bleed onto the shared `.cl-*` grid) + reworded the lone `<style>`-substring comment (classification `<style>`-ban); two variadic `.append(...)`→`appendChild` loops (apps/web's Cloudflare worker-types shadow DOM `Element.append` with HTMLRewriter's string-only one). **Comments attachments + reactions NOT surfaced this slice** (separate `bo_at_comment_attachments` read → follow-up; panel degrades to 0-file count). NOT synced through apps/design (promoted direct to web — harness already carries the fork family). |
| 2026-08-13 | `7c7202d7` | (web-notifications-inbox full-page) | SCOPED promotion — the NET-NEW full-page Inbox (`views/InboxView.astro` → `/inbox`), the fork's TRIAGE redesign of the notification centre (155px rows in a 384px overlay could not be scanned → same rows/lanes/filters given a page's width). Promoted `InboxView.astro` VERBATIM + the single `asPage` render adaptation. In apps/web the sidebar dropdown STAYS live (the fork deleted it; we keep both), so the shared `components/layout/Inbox.astro` keeps its overlay `<aside hidden>` default and gains a backward-compatible `asPage` prop → page mode renders `<section class="ibx ibx-page">` (no overlay id/hidden/close button, static positioning, `.ibx-page` CSS appended to the existing `<style is:global>` block so the classification `<style>`-ban still passes). `inbox-client.ts` reads `data-as-page` and skips the open/close/Esc controller only — tabs/filters/triage/mute/`sync()` and every row's `data-space-id`/`data-base-id` (the triage/mute route addresses) stay shared verbatim. New `pages/inbox.astro` SSR-loads the SAME `fetchInboxItems` fan-out the dropdown uses (resolved once → both the layout badge and the page body); `SidebarLayout` gains `showInbox` (default true), `/inbox` passes `showInbox={false}` so the page panel is the SOLE `[data-inbox]` inbox-client wires (two collide). Triage/mute reuse the EXISTING `/api/spaces/:spaceId/inbox/{triage,mute}` proxies — NO new endpoints. `Inbox.stories.ts` gains an `AsPage` story. NO `/inbox` nav entry (the fork's `app-config.json` adds none; repointing the bell = a dropdown-surface change, out of atomic scope). NOT synced through apps/design (promoted direct to web). |
| 2026-08-17 | `986f6c09` | (web-ui-sync-promotion · Stage-1 sync) | **FULL design-harness sync of the 45-commit convergence wave** (`7c7202d7`..`986f6c09`) into `apps/design`. Mechanism: 183 verbatim `git show` copies of the delta + **dependency-closure backfill at the pinned hash** — the converged views drag in fork modules that predate `7c7202d7` and were never localized, so ~31 missing `apps/web/src/lib/*` + ~27 missing `apps/web/src/components/*` were backfilled into `apps/design/src/{lib,components}` to make the closure resolve (incl. the **deferred Schema re-arch** cluster `SchemaView`+`UndoToast`+… — lands in the HARNESS only; promotion still deferred per §4). 8 fork deletions mirrored (panel-lab/prototype pages, research prototypes). **One new runtime dep: `apexcharts@^5.3.4`** (fork pin) added to `apps/design` — the Reports KPI `TrendChart`; will ride to `apps/web` at the Reports promotion (§3.3 security callout: widely-used MIT charting lib, `minimumReleaseAge`-gated). `apps/design/src/lib/ui.ts` shim widened to `export * from '@web/lib/ui'` (fork added `cn`/`inputId` alongside `CONFIRM_DESTRUCTIVE`/`setButtonLoading`). §2 permitted rewrites applied to 18 `apps/design/src/pages/**`: `@web/views/*`→relative + `@web/components/schema/*`→relative (depth-correct) so the harness renders the localized converged copies (our `@web`→apps/web lacks the un-promoted views, e.g. Reports). `audit/**` (57) → `apps/design/audit/` + `specs/16-responsive.md` → `apps/design/specs/` as convergence reference (feeds the Phase-1 gate-vs-build ledger). **DEFERRED / not synced:** `apps/support` (net-new app, client-blocked Q#6, self-contradicting research `fdcc4acf`) + `brand/baseout-bridge.css` (serves it); `support-portal` openspec research imported as reference only (`README-IMPORT.md` annotates the block). **storybook.ts 3-way DEFERRED** (25 upstream entries incl. rename supersedes `empty-state`→`pattern-empty-state`, `page-header`→`pattern-page-header`, `back-link`→`pattern-back-step` — a naive graft dupes; precedent: `9a8b448` deferred ~1.6k upstream entries to a later sync). Gates: **`pnpm --filter @baseout/design build` GREEN** + **`pnpm --filter @baseout/web audit:components` exit 0**. Promotion-time (NOT design-copied): `apps/web/src/{styles,layouts,pages,components/layout,public,design-assets}/**`, `app-config.json`. NOT yet committed (human-tested loop). |

| 2026-08-19 | `252005be` | **(triaged — PLANS FILED, nothing synced yet)** | **22-commit delta triaged, not synced.** `pnpm ui:sync-status` classifies it as: **2 never-sync** (`.claude/hooks/smoke-support.mjs`, root `package.json`) · **68 unmapped** = the net-new **`apps/support`** portal (99 files incl. 14 binaries) + `brand/baseout-bridge.css` + fork-root `DESIGN.md`/`PRODUCT.md` · **1 web layer** = `components/ui/PanelHost.astro` · **4 openspec** = `support-portal` research refresh. Scoping decisions taken with the human 2026-08-19: Schema re-arch stays deferred (gated tabs only) · support portal IS in scope, `prelaunch`/`survey` are NOT · client-blocked surfaces get plans with the blocker named · the design audit becomes an umbrella. **Five new changes filed** — [`support`](../../openspec/changes/support/) (net-new app; **blocked on client #6**), [`web-data-docs-chat`](../../openspec/changes/web-data-docs-chat/) (Data ▸ Docs + Chat un-gate; both backends already exist), [`web-auth-convergence`](../../openspec/changes/web-auth-convergence/) (the Login/Welcome half `2bb5b46a` held back + **ship item 1**, the unsubmittable auth form), [`web-design-audit-convergence`](../../openspec/changes/web-design-audit-convergence/) (the 41-item ship order; **`PanelHost.astro` — the delta's only web file — lands here as item 5**), [`web-billing-usage`](../../openspec/changes/web-billing-usage/) (**blocked on client #4 AND on a fork design that does not exist**) — plus reconcile notes on [`web-reports-page`](../../openspec/changes/web-reports-page/) (in-flight, ~10/14, only routing+nav left), [`web-automations-interfaces-tabs`](../../openspec/changes/web-automations-interfaces-tabs/) (the last full-stack build — no engine route exists) and [`web-settings`](../../openspec/changes/web-settings/) (§5, the remaining not-real rows). **storybook.ts 3-way still deferred** (25 upstream entries, carried from `986f6c09`). |
| 2026-08-20 | `9cf5b1ef` | **(program branch — plans only)** | Branch `autumn/cursor-ui-implementation-test` cut from `origin/main`. Fetched ui-only tip `9cf5b1ef` (+11 since `252005be`, mostly `apps/support` polish). Child plans restored + umbrella [`web-cursor-ui-implementation`](../../openspec/changes/web-cursor-ui-implementation/). **Nothing synced.** Note: `main` still lacks the 17 commits on `web-ui-sync-promotion` — Phase 0 decision is absorb that branch vs rebuild. |
| 2026-08-20 | `9cf5b1ef` | (Slice A Task 5 · this commit) | **Data A1 wiring** on `autumn/cursor-ui-implementation-test`: `DataView` + `data.astro` mount real `DataBrowse` / `DataChangelog` / `DataMedia` (+ `RecordPanel` / `MediaPanel` / `PanelHost`) over engine SSR page-1 via `lib/data-browse/map` + existing `/api/spaces/:id/data/*` proxies. ui-only tab clusters (Records · Attachments · Comments \| Changelog \| Docs · Chat). **managed_pg / static gates honest** (no fixtures). Comments / Docs / Chat stay SoonTab until A2. §4 Data row updated — Browse/Changelog/Attachments → REAL. |
| 2026-08-20 | `9cf5b1ef` | (Slice A Tasks 9–11 · this commit) | **Data A2** on `autumn/cursor-ui-implementation-test`: **Comments** — `DataComments.astro` promoted (classified + storied; `is:global` styles); SSR `getDataComments` + `mapComments`; same managed_pg/static gates as Attachments; comments passed to Browse comment-presence filter + RecordPanel. **Docs + Chat** — `dataToSchema.ts` (+tests) projects Data → DocsTab entities / Chat healthBases; additive `scope`/`noun` on Schema `DocsTab`/`ChatTab` (Schema call sites unchanged); Data mounts with `scope="data"`; SSR `listDocuments` + `listChatThreads`; Pro+ chat gate inherited. **Zero SoonTabs** on DataView. Follow-ups: comment attachments/reactions read, Media cursor pagination polish. |
| 2026-08-20 | `4073f919` | (Phase 4 · web-settings · this commit) | **Settings hub re-landed** on `autumn/cursor-ui-implementation-test` (did **not** merge `web-ui-sync-promotion`). Wiring matches promotion `c8a78057` at design hash `ui-only@7c7202d7` (tip `4073f919` has a later SettingsView; deferred to keep token/AI/billing contracts identical). Promoted `views/{SettingsView,settingsCatalog,settingsControls,ApiTokensPanel,AiKeysPanel,SecurityPanel}` + legacies + `lib/auth/securityPanel.ts`. Deps for gated SecurityPanel: `ui/CodeInput` (+story+classification+`code-input` SB entry) + `lib/auth/{types,codeInput}` + `code-input.css`; `CONFIRM_DESTRUCTIVE` + ConfirmModal `value="confirm"`. `pages/settings.astro` thin SSR loader; `settings/ai-keys` → `?tab=developer`. **LIVE:** tokens, AI keys, billing portal, Space retention/schedule/destination links. **GATED:** account/org/space prefs, notifications (no `/api/account/notifications` on this branch), Security 2FA pane (honest note). `ENROLLED_WORKSPACES` fixture dropped. Caveat: usage→`/reports` still V1-trims to `/`. |
| 2026-08-20 | `4073f919` | (Phase 5 · web-auth-convergence · this commit) | **Auth visual convergence** on `autumn/cursor-ui-implementation-test` (did **not** merge `web-ui-sync-promotion`). `styles/components/auth.css` → ui-only scroll contract (`.auth-layout` min-height 100dvh; centring via `.auth-card` `margin-block:auto`; `.auth-brand` keeps `overflow-hidden`). Background → `public/images/auth-rays.webp` (fork); `auth-bg.png` retired. Promoted `views/{LoginView,WelcomeView,AuthAssociationView,welcomeForm}` — fork markup + real better-auth/onboarding wiring (no fixture scripts); email-format refusal before magic-link; house-voice copy. S36-F3 `#too-narrow` 390 floor into `Layout.astro` + global.css. **RegisterView SKIPPED** (working `pages/register.astro`). NO `lib/auth*.ts` / `pages/api/auth/*` / middleware changes. Caveat: magic-link E2E is deploy-only. |

## 4. Promotion status matrix

| surface | synced at | promoted in | data wired | legacy rollback |
|---|---|---|---|---|
| Schema shell + 8 tabs | `d97c777` | `09949d3` (web-schema-round3-shell) | yes — SSR `schema.astro` via `createBackupEngine()` | pre-split SchemaView in git history |
| Schema Visualize / Changelog tabs | `d97c777` | in-flight on `autumn/june-ui-refactor` (web-schema-visualize / web-schema-changelog) | changelog feed live; visualize from SSR payload | — |
| Schema export control | `3153dfd` | `web-schema-export` (patterns/ExportControl + lib/csv, mounted on all 7 Schema tabs) | Browse + Changelog export REAL CSV client-side (`schema:export` event + `export-rows.ts`); other tabs honest-fallback until their backends | — |
| EntityPanel anchor model + panel polish | `3153dfd` (design harness) | **STAGED — web mount deliberately deferred** (2026-07-10): the type-import closure drags 3 fixture-driven tab components into web; draft/publish/generate actions are fixture-faked upstream, so mounting would replace the real docs-by-entity panel with faked actions; upstream drawer-round4 client questions still OPEN. 19 styleguide entries for un-promoted surfaces carry `design:`-prefixed references (the resolvability gate skips them; retarget at promotion). **2026-08-11 (design-descriptions-readonly): the Airtable description tab is now READ-ONLY in the harness — the faked Publish/Discard-to-Airtable flow, the `descStates` publish states, and the `airtableDraft`/`airtableExternallyChanged` seed on `Deals ▸ Stage` were removed; Internal stays editable. This DIVERGES `EntityPanel.astro` + that fixture + the storybook.ts `pattern-entity-panel` entry from `ui-only`. **`ui:sync-drift` then revealed the fork ALREADY did this, more completely, in `ui-only@7502f81` ("Airtable goes read-only, and writing gets its own section", Oleh 2026-08-07) — lifecycle deleted across panel + controller + Browse tree + both type sets. So the resolution is a FORWARD-sync of `7502f81` that SUPERSEDES this hand-edit — NOT a reverse-sync UP. Our edit is a buildable interim until that forward-sync lands.**} | pending — superseded by `7502f81` on the next schema forward-sync | current BrowseTab panel is the working fallback (already read-only) |
| Actions landing (read-only "writing gets its own section") | `7502f81` (design harness) | **`web-actions` (2026-08-11): promoted `ActionsView.astro` VERBATIM into `apps/web` (self-contained daisyUI, bespoke=0) + `lib/actions.ts` examples + `app-config.json` nav (`/actions`) + Features §1 `Action` dict** | landing only — static example copy; feedback form is a no-JS GET placeholder (POST+store is a follow-up) | `agents.astro` (the replaced hand-built draft) deleted |
| Account-level Sources (registry + add + detail) | `7c7202d7` | **`web-sources` (2026-08-13): promoted `views/{SourcesView,SourceAddView,SourceDetailView}` VERBATIM into `apps/web` (daisyUI-direct behind the raw-markup allowlist; one strict-tsc `connecting` cast in the detail view) + deps `lib/registry/removal.ts`, `components/integrations/AccessScopeNote.astro` (+story+classification), `lib/ui` `CONFIRM_DESTRUCTIVE`, `ConfirmModal` `value="confirm"`, shared `.tbl-frame/.page-head/.sch-slot(-in)/.sch-cap-mark/.sch-modeswitch/.asn*` in global.css** | **yes** — SSR via `getIntegrationsState → toSourceSummary`; the new `accessLostAt` prop reads `connections.invalidatedAt ?? pendingReauthAt`, `addedAt` reads `createdAt`, `lastChecked` gated `null` (no source column). One source today (active Space's Airtable connection) mapped into the registry with an honest account-level-pending notice; account-level multi-source persistence + source↔Space linking remain `shared-sources` (see also `web-nav-ia-restructure`). Connect/reconnect/disconnect keep the existing `api/connections/airtable/*` contract | `SourcesView.legacy.astro` · `SourceAddView.legacy.astro` · `SourceDetailView.legacy.astro` |
| Account-level Destinations (registry + add + detail) | `7c7202d7` | **`web-destinations` (2026-08-13): promoted `views/{DestinationsView,DestinationAddView,DestinationDetailView}` VERBATIM into `apps/web` (daisyUI-direct behind the raw-markup allowlist; NO strict-tsc cast — `DestinationStatus` already carries all 4 lifecycle states). The direct twin of `web-sources`; ZERO new deps — `lib/registry/removal` (already handled `kind:'destination'`), `CONFIRM_DESTRUCTIVE`, `ConfirmModal value="confirm"`, `schema/tableSort`, and the shared `.tbl-frame/.page-head/.sch-*` global classes all landed with `web-sources`.** | **yes** — SSR via `getIntegrationsState → toDestinationSummaries` over the real per-Space `storageDestinations` (+ provider-catalog meta); every prop the redesign reads was already populated, so NO store/mapper/schema change. Pages `destinations.astro` + `destinations/new.astro` trimmed to the fork prop contract; `destinations/detail.astro` already matched. **Swap-primary + the "what you can connect" provider grid DROPPED from the account registry view** (they belong to the per-Space Backups config / `shared-multi-destinations`, preserved there + in the legacy rollback); account-level model + connect path remain `shared-destinations`. Connect/reconnect/disconnect keep the existing `api/connections/storage/*` contract | `DestinationsView.legacy.astro` · `DestinationAddView.legacy.astro` · `DestinationDetailView.legacy.astro` |
| Space Home / dashboard | `7c7202d7` | **`web-space-home-dashboard` (2026-08-13): promoted the fork's grown `views/SpaceHomeView.astro` (28KB→57KB) into `apps/web` (old→`.legacy.astro`). New dep GRAFTED into `lib/backups/format.ts`: `deriveSpaceHealth`/`isInFlightRun`/`runKind`/`runMovedAt` + health types (additive; nothing dropped; `formatNextScheduledAt`↦`lib/time` aliased) + unit tests. `SpacePipelineHero` (patterns/) widened to nullable `spaceName`. `RunBackupNowModal` kept on apps/web's real POST contract; `.hm-kpi*` kept scoped (§1.5). DashboardView RETIRED → `.legacy.astro`.** | **yes** — `pages/index.astro` UNCHANGED (identical Props): real feed via `listRecentRuns` + `getIntegrationsState` + `toSourceSummary`/`toDestinationSummaries`; the page-level 2s backup-status poll (`data-backup-history` wrapper + home-live) SURVIVES the swap (it wraps the view, keyed off page-level DOM, not view internals). Honestly GATED empty (in-code): `baseMetrics=[]` (Schema section hidden) + `usage=[]` (Usage section hidden) — no engine/billing feed yet; the view hides both when empty. `<LiveRefresh>` = an additional manual control, not a replacement for the poll | `SpaceHomeView.legacy.astro` · `DashboardView.legacy.astro` |
| Backups (list + run detail + base) | `7c7202d7` | **`web-backups-redesign` fwd-sync (2026-08-13): grown `views/{BackupsListView,BackupRunDetailView,BackupRunBaseView}` promoted (old→`.legacy.astro`). FacetFilter + refineCollapse DROPPED (diverged schema cluster) → search + click-sort + pager via `createRunLog`; live poll re-grafted via the `home-live` reload-on-transition strategy. Deps: `backups/copyId`, `schema/entityIcon`, `ui/Drawer` (style→global.css + story + classification), `format.backupRunTitle` (+tests). SKIPPED, imported by no run-view (Integrations surface): BackupHistoryWidget/CadencePicker/BackupScheduleScope/RunBackupButton/StoragePicker.** | **yes** — `backups.astro`→`listRecentRuns` (poll wrapper + DOM ids UNCHANGED; only `spaceId` added to the view call); `run.astro`/`base.astro`→`engine.getRunDetail` mapped into the new `RunDetail`/`BaseDetail` shapes (`error`/`outcome`/`createdAt`/`queued` from the DB row; `metricsPending` fallback = run-level totals + note); real `RunBackupNowModal` (`spaceId`+`redirectTo`). Faceted-filter DROPDOWNS gated (schema-surface follow-up); actions (cancel/delete/retry) unchanged | `BackupsListView.legacy.astro` · `BackupRunDetailView.legacy.astro` · `BackupRunBaseView.legacy.astro` |
| Auth (login · welcome · 2FA challenge · domain-association) | `4073f919` (shell+Login/Welcome); wiring `7c7202d7` | **`web-auth-convergence` (2026-08-20) on `autumn/cursor-ui-implementation-test`:** auth.css scroll contract + `auth-rays.webp` (retired `auth-bg.png`); grown `LoginView`/`WelcomeView` + `welcomeForm.ts` (+tests) + `AuthAssociationView`; 390 floor in `Layout.astro`. Prior `web-login-methods` wiring preserved (magic-link / onboarding / join-request). RegisterView SKIPPED; AuthChallengeView still the minimal `/2fa` page on this branch (full challenge view not re-landed here). | **yes** — same backends as web-login-methods. Email-format refusal is UX-only before `signIn.magicLink`. SSO still honestly gated on server config. **Deploy-only:** real magic-link round trip. | `LoginView.legacy.astro` · `WelcomeView.legacy.astro` |
| Settings hub (account · security · org · space · billing · notifications · developer) | `7c7202d7` (wiring); tip `4073f919` noted | **`web-settings` re-landed 2026-08-20 on `autumn/cursor-ui-implementation-test` (Phase 4; no promotion-branch merge): same as 2026-08-13 promotion — `views/{SettingsView,settingsCatalog,settingsControls,ApiTokensPanel,AiKeysPanel,SecurityPanel}` + `lib/auth/securityPanel.ts` + CodeInput dep stack. `pages/settings.astro` thin loader; `ai-keys` → hub. `ENROLLED_WORKSPACES` dropped.** | **yes (4 sections)** — tokens + AI keys + billing portal + Space links (`/retention` `/backups` `/destinations`). GATED: account/org/space prefs, notifications (no prefs API on this branch), Security 2FA (panel promoted, honest note). usage→`/reports` still redirects home (V1 trim) | `SettingsHub.legacy.astro` · `AiKeysView.legacy.astro` |
| Data page (Records · Attachments · Changelog · Comments · Docs · Chat) | `9cf5b1ef` | **Slice A on `autumn/cursor-ui-implementation-test`:** Task 4 promoted fork `components/data/*` + closure. Tasks 5 + 9–11 wire all six tabs. | **ALL TABS REAL** — Browse / Changelog / Attachments / Comments via engine SSR + `/api/spaces/:id/data/*` proxies + `lib/data-browse/map` (incl. `mapComments`). Docs / Chat reuse Schema `DocsTab` / `ChatTab` with `scope="data"` + `dataToSchema` entities (`web-data-docs-chat`). **managed_pg / static gates honest**. Launch Docs entitlement + Pro+ chat gate inherited. **Follow-ups (honest gaps):** comment attachments + reactions not in first read slice; Media deeper cursor polish. Zero SoonTabs on DataView. | `DataView` phase-1 shell in git history (edited in place; no `.legacy`) |
| Notifications Inbox (dropdown) | `3153dfd` | `web-notifications-inbox` §1–4 (Inbox + inbox.ts + inbox-client verbatim; AppShellHeader bell removed/`lg:hidden`; AppShellSidebar trigger + badge; SidebarLayout `inboxItems` prop default `[]` → designed zero-states) | **LIVE end-to-end** (2026-07-10): engine feed `GET/POST /api/internal/spaces/:id/notifications{,/triage,/mute}` (derive-not-mailbox, per-Space `bo_at_inbox_state`/`bo_at_inbox_mutes`, SPACE_SCHEMA_VERSION 6) + web fan-out in SidebarLayout + optimistic triage via `/api/spaces/:id/inbox/*` proxies. Deferred: health-drop kind (needs debounce), per-user read state, web §5.2-5.4 | Header/Sidebar pre-inbox in git history |
| Notifications Inbox (full page) | `7c7202d7` | **`web-notifications-inbox` §7 (2026-08-13): promoted net-new `views/InboxView.astro` → `/inbox` (fork verbatim + `asPage` render). Shared `Inbox.astro` gained backward-compat `asPage` prop (page `<section class="ibx ibx-page">` vs overlay `<aside hidden>` default — dropdown byte-for-byte unchanged); `inbox-client` skips only the open/close/Esc controller in page mode; `SidebarLayout.showInbox` suppresses the dropdown on `/inbox`; `Inbox.stories.ts` +`AsPage`.** | **yes** — `pages/inbox.astro` SSR-loads the SAME `fetchInboxItems` per-Space fan-out (resolved once → layout badge + page body); triage/mute reuse the EXISTING `/api/spaces/:spaceId/inbox/{triage,mute}` proxies (NO new endpoints; data-space-id/data-base-id preserved) → same optimistic persistence as the dropdown. NO `/inbox` nav entry (fork adds none); §7.7 bell-repoint deferred | dropdown stays live (both coexist); no legacy file (net-new surface) |

**Schema surface — re-architecture DEFERRED (decision 2026-08-14, "easiest + most secure").**
The full ui-only Schema promotion program stops at the live shell + 8 tabs (already
promoted, working on real data). The fork RE-ARCHITECTED Schema into ~24 dedicated
components (`SchemaBrowse`/`SchemaChangelog`/`RelationshipPanel`/`SchemaCanvas`/
`EntityPanel`/`QuickAskDock`/…) that apps/web lacks; promoting them would re-architect
a live surface (regression risk on the densest screen, §1.5) AND collide with the
dedicated in-flight `web-schema-*` changes on `autumn/june-ui-refactor`
(visualize/changelog/chat/relationships/health). So Schema's component re-architecture
+ EntityPanel read-only supersede + Automations/Interfaces backend are OUT of the
`web-ui-sync-promotion` program and stay on their own track. FacetFilter + a few schema
closure files DID land in apps/web as net-new deps of the Data page (`web-data-page`),
with no live-tab conflict.

## 5. Known traps

- **Must-refresh polling regression:** promotions that swap the view
  mounting the backup-status widget drop the 2s poll wiring
  (`apps/web/src/stores/backup-runs.ts` + widget lifecycle). Re-verify after
  every app-shell or Integrations-adjacent promotion.
- **Verbatim tsc errors:** upstream `.ts` files carry strict-mode errors —
  leave them; `pnpm --filter @baseout/design build` is the gate. Fixing them
  breaks diffability.
- **storybook.ts reconcile:** never overwrite blindly (see §2).
- **EntityPanel write-back — the fork already removed it (7502f81):**
  `design-descriptions-readonly` hand-removed the faked Airtable write-back from
  `apps/design/.../EntityPanel.astro` (a buildable interim). Then `ui:sync-drift`
  showed `ui-only@7502f81` ("Airtable goes read-only, and writing gets its own
  section", Oleh 2026-08-07) had ALREADY done it, more completely (panel +
  controller + Browse tree + both type sets). **Resolution = FORWARD-sync
  `7502f81` to SUPERSEDE the hand-edit — do NOT reverse-sync ours up.** Until
  then, a forward sync of `EntityPanel.astro` / the `Deals ▸ Stage` fixture / the
  storybook `pattern-entity-panel` entry is SAFE (it brings the canonical
  read-only version) — the earlier "must not re-import" concern is void now that
  the fork's version IS the read-only one. `web-actions` already promoted the
  paired Actions landing from the same commit.
- **Header/Inbox entanglement:** the Inbox surface edits
  Header/Sidebar/SidebarLayout — an app-shell change. Never land it
  half-promoted (design and web must agree on which shell renders).
- **astro/@astrojs/node version pairing:** older pairs break; the 7-day
  `minimumReleaseAge` gate picks versions (see `53110f8`).
- **Dirty targets:** never sync onto uncommitted local edits of the same
  file — `pnpm ui:sync-status` flags these; commit first.

## 6. Update rules

- New sync → add a §3 row (replace the *pending* row) + update §4 rows it
  affects, same commit.
- New promotion → update the §4 row (promoted in / data wired / legacy),
  same commit.
- New trap discovered → add to §5 in the change that hit it.
- Cite `ui-sync.md §N` in openspec proposals and commit bodies when a
  decision leans on this file.

## 7. Handoff (a surface the fork LACKS → plan + code)

The three directions of work over this ledger:

| direction | case | tool |
|---|---|---|
| fork → web | pull/promote a fork design | `/ui-sync` + `pnpm ui:sync-status` |
| web → fork | an EDIT to a surface the fork already has | `/sync-reconcile` + `pnpm ui:sync-drift` (a patch UP) |
| web → fork | a UI/UX surface the fork **LACKS** (admin app; implement-before-design) | **`/spec-handoff`** (an OpenSpec plan **+ code** UP) |

`/spec-handoff` is the **rare** case: we developed UI/UX that isn't in the `ui-only` branch **at all**
— a whole surface he's never had (the **admin app**, `apps/admin`), or one we implemented before he
designed it. The deliverable is a **self-contained OpenSpec implementation plan the designer runs
himself** (his `opsx:apply`, on his timeline — we never implement it for him), **additive so it
applies from wherever his branch currently is**. Because he is **designing around our
implementation**, it carries the **relevant UI code** (views/components/styles/layouts — not
backend/db/auth) **plus a `docs/` artifact explaining the changes** for him to view **after successful
implementation** — not just a spec. It is authored as a fork-side `openspec/changes/<x>/` (invert the
§2 `web-` prefix) **bundling that code + docs**, staged as a `change/<x>` branch (or `patches/ui-only/**`
patch) for a **human to upload** — a handoff branch, **never a merge** into his working branch.
Presence check: `git ls-tree -r ui-only/main -- <path>`; if the fork already has it, it's `/ui-sync`
or `/sync-reconcile`, not this. Same never-do rules as §1: never merge into `ui-only`, never
auto-push, never a PR, never `git merge ui-only`.

**Recording a handoff:** add a note to the surface's §4 row (or a new row) marking it *handed off →
fork*; add a §3 row once the human confirms the upload (§6). A brand-new surface with no §2 mapping
(admin) proposes its fork location in the handoff for the designer to confirm. Procedure:
`.claude/skills/spec-handoff/SKILL.md`; scope: `openspec/changes/system-spec-handoff/`.
